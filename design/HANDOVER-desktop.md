# Handover — the desktop, last session only

> **One file per machine, one session each. It is replaced each time, not added to.**
> This machine writes only this file; the laptop's is
> [`HANDOVER-laptop.md`](HANDOVER-laptop.md), and a session reads BOTH at startup.
> Anything worth keeping beyond tomorrow belongs in the feature's Brief or on
> [`features/to-do-items.json`](features/to-do-items.json). Earlier handovers are in git
> history. See [`WORKING-AGREEMENT.md`](WORKING-AGREEMENT.md).

---

## No desktop session has been recorded here yet

This file was created on **2026-08-31**, when the single `HANDOVER.md` was split in two so
that both machines can work at once without overwriting each other's note. The desktop
writes here from its next session onward. Until then this is not a gap in the record — it
is a file waiting for its first entry.

**What the desktop needs to know today is in the laptop's note.** The 2026-08-30 entry in
[`HANDOVER-laptop.md`](HANDOVER-laptop.md) opens with a block addressed to this machine:
`master` moved for the first time since 22 August, nothing the desktop has ever pushed is
stranded, and there are three commands to run before starting — the third is the one that
matters, because it is the only way to see desktop work that was committed but never
pushed.
