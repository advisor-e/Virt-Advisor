# Virt Advisor — Action Backlog ARCHIVE (completed work)

> **This is the history half of the action backlog.** Every item here is **done** (`✅`).
> The live, open list is [`ACTIONS.md`](ACTIONS.md) — triage from there.
>
> **Append-only.** Items move *into* this file when completed; they are not edited here
> afterwards. Commit refs, dates, and reasoning are preserved verbatim from when they were
> closed, so a saved decision stays auditable. See memory `feedback-no-silent-parking`.

---

## Resolved blockers & historical context

> **✅ DISPLAY-DROP DEFECT — FIXED 2026-06-25 (Stage 6 made deterministic; the diagnosis in ACTIONS was wrong).**
> **Symptom (live crisis-café session `a10ffb66`, 2026-06-24):** `va-session` logged `topTemplate "Cafe" topScore 29` yet the cards shown were *Receivership vs Liquidation* (framed "above your usual advisory range"), *Quick & Worst*, *8 Profit Levers* — `Cafe` (the highest score) was dropped.
> **Diagnosis CORRECTED:** the open item blamed `resolveTemplatesWithOutlier` / a complexity-ceiling classification dropping `Cafe`. A ground-truth repro (real `templates.json`, the documented caseState) **disproved that**: both passes returned `Cafe (29), Quick & Worst (22), Receivership (22)`, all in unblocked subSections, `hasOutlier:false` — so the resolver never dropped `Cafe` and never produced any "above-range" framing. The real cause was downstream: **Stage 6 (`advisorEngine.js`) handed the AI a wide ~12-template candidate net and instructed it to *pick* the final cards** (`CANDIDATE TEMPLATES … choose only from these … aim for N`), plus R17 licensed the AI to *exclude* candidates "no matter how they scored." So the **AI** dropped the engine's #1 and invented the stretch framing — a violation of System-Design Principle 4 ("Code makes macro-decisions. AI writes copy only") and the Stage 6 contract (§3.7: the AI cannot select/add/substitute).
> **Fix (Option A — "code decides, AI writes copy"):**
> 1. New pure, exported helper `buildDisplaySet(resolvedResult, budget)` in `templateResolver.js` — turns the two-pass output into the FINAL ordered card set (outlier-led when `hasOutlier` per §13, else the unrestricted top-N; deduped; budget-capped). Domain-agnostic by construction (reads only the result shape). The engine's #1 can never be dropped.
> 2. `advisorEngine.js` Stage 6: feeds the AI exactly `_displayTemplates` (was the wide net); `preFilteredNames` now derives from it (so summaries + `buildClientContext` library context both collapse to the chosen set); the candidate-net prompt block was replaced with `RECOMMENDED TEMPLATES — write up EVERY one, no add/drop/reorder/substitute`; the outlier block became framing-only (the set is already fixed).
> 3. `data/prompts/client.txt` R17 retired — was the AI's exclusion licence; reframed to "Fixed recommendation set" (write up exactly what the engine listed; correct selection at the engine, not here). Industry-mismatch is already handled deterministically by the resolver's −15 wrong-industry hold-back. **This also closes the old P3 "retire the R17 relevance gate once scoring matures."**
> 4. Tests: `tests/unit/displaySet.test.js` (7) — top-scored never dropped, outlier-led ordering, dedupe, budget cap, budget defaulting, empty→fallback, and the café-crisis acceptance case (`Cafe` always in the set). **Full suite 518/518 green; lint 0 errors; cross-domain `selectionHarness` snapshots unchanged (resolver untouched).**
> **Why this matters beyond the bug:** it makes the Advisory Distinctions lever *reliable* — a firm's boost that ranks a template #1 is now guaranteed to surface (previously the AI could silently overrule it), and the decision-trace now matches what the advisor actually sees. Files: `server/utils/templateResolver.js`, `server/advisorEngine.js`, `data/prompts/client.txt`, `tests/unit/displaySet.test.js`.



> **★★ IP DEPTH AUDIT — DONE + FOLD-INS EXECUTED 2026-06-20.** Full audit ran
> (pdftotext extract → depth-ratio screen → corpus-level named-concept coverage → read-verify).
> **Findings + the verified genuinely-missing list + the executed fold-ins are in
> `design/SESSION-2026-06-20-IP-DEPTH-AUDIT-NOTES.md`.** Headline: coverage broad, depth mostly
> sound; gaps were short + modest + clustered in DORMANT specialist trees. A methodology trap was
> caught (IP folded into a SIBLING file looked "missing" — People Power lives in `staff_performance`,
> get_seminar methods in `powerful-seminars.json`); the screen now checks the whole corpus.
> **8 fold-ins applied (transcribed faithfully from the source PDFs, no invented IP); 1 LIVE
> (ratio_analysis named ratios + a formatter block in `logicTrees.js`), the rest dormant-and-ready.
> dashboard_discussions verified NO change needed. 421/421 tests pass, lint clean, all JSON valid.**
> Files touched: `data/ratio-analysis-reference.json`, `get-seminar-`, `raising-capital-`,
> `fm-coach-culture-`, `valuation-`, `org-leadership-`, `org-board-pack-`, `org-firm-strategy-domain-support.json`,
> + `server/utils/logicTrees.js`. **Dormant fold-ins only reach the AI when those trees go live (see
> the 28-dormant-trees item).**

> **✅ STACK-RECONCILIATION INSTALL — UNBLOCKED 2026-06-12.** The branch installs to **exit 0** and the full suite passes **(272/272) on Node 14.15**. Resolution chain:
> 1. **`isomorphic-dompurify` contradiction — RULED ON by the coding team (2026-06-11):** pin to the Node-14-compatible **`1.3.0`** (keeps the spec-named package *and* Node 14.15; `1.3.0`'s `jsdom@21` allows `node>=14`, and it still ships a modern DOMPurify 3.x). Exact pin, no caret (`1.11+` needs Node ≥18).
> 2. **Avast antivirus TLS interception** broke `npm install` (`UNABLE_TO_VERIFY_LEAF_SIGNATURE`) — Avast's "Web/Mail Shield" re-signs HTTPS and Node doesn't trust its root. Fixed by exporting the Avast root CA to `C:\Users\<user>\avast-root-ca.pem` and setting **`NODE_EXTRA_CA_CERTS`** to it for installs (additive; nothing weakened — same cert Windows already trusts).
> 3. **`formidable` pinned `^3.5.4 → 2.1.2`** — v3 pulls `@paralleldrive/cuid2 → @noble/hashes@1.8.0` which wants Node 14.21.3; `2.1.2` uses `hexoid` (no such chain) and runs on 14.15. Required a small callback→promise wrapper (`parseForm`) in `server/routes/firmManager.js` since v2's `parse()` is callback-style. File-object prop names are identical v2.1↔v3, so the upload/DB code is unaffected.
> 4. **`@nuxt/friendly-errors-webpack-plugin` pinned to `2.5.2`** via `overrides` (its `2.6.0` wants Node 14.18). **`overrides` only take effect under npm 8** (which itself runs on Node 14.15) — the bundled **npm 6 ignores `overrides`** — so installs on this branch use a local npm 8 (`C:\Users\<user>\npm8tmp`). The committed **`package-lock.json` (lockfileVersion 2)** encodes the pinned tree, so reinstalls reproduce it.
> 5. **`engine-strict` relaxed `true → false`** in `.npmrc` — see the "Dev-toolchain drift past Node 14" P1 (still open in `ACTIONS.md`). The app RUNTIME is Node-14.15-clean; the only mismatches are ~47 dev/build tools.
> 6. **Live boot test (2026-06-12)** caught two runtime libraries the install/tests missed (both use `node:`-prefixed requires, a Node ≥14.18 feature): **`googleapis` pinned `^144.0.0 → 123.0.0`** (last release on `google-auth-library` v8; Drive usage in `driveService.js` is basic files/folders CRUD, identical API, no code changes) and **`mysql2` pinned `^3.22.3 → 3.16.0`** (last clean release before `node:` usage). **Result: the Restify backend BOOTS and SERVES on Node 14.15** (listening :4000; protected route returns the standard 401 envelope).
>
> **Install command (this branch):** `NODE_EXTRA_CA_CERTS=C:\Users\<user>\avast-root-ca.pem  <node-14.15>\node.exe  <npm8>\node_modules\npm\bin\npm-cli.js  install`
>
> **✅ RESOLVED 2026-06-15 (was the 2026-06-12 `openai ^4.x` ⊥ Node 14.15 referral): coding team ruled** — keep Node 14.15, drop the OpenAI SDK, call the OpenAI REST API directly from the Restify backend (keys backend-only). Req 7 amended in `CLAUDE.md`. Separately, the **dev-server boot is now unblocked on Node 20** via telemetry-off + `@babel/preset-env@7.10.4` (commit `5d39966`) — `npm run dev` serves on :3000 for live testing.

---

## Completed — P1 items

- ✅ **SEC/DATA — Shape-validate AI JSON output (the grade path drives course pass/fail). DONE 2026-06-15 [P1 #1].** The live AI JSON parses were guarded against *parse failure* (try/catch) but NOT against *valid-JSON-wrong-shape*: `course.js:424` (quiz-grade) returned the model's `passed`/`score`/`feedback` straight through with no type/range check — an advisor's course pass/fail rode on unvalidated AI output. Same class: `course.js:385` (quiz-generate), `advisor.js:88` (distinction-classify, partly checked via `Array.isArray`). **Done:** `validateQuizGenerate` + `validateQuizGrade` added to `validateAIResponse.js` (100% coverage retained), wired into both `course.js` quiz handlers; an invalid shape now routes to the existing fail-safe error (never auto-passes). +30 tests, full suite 302 pass. **✅ Orphans RESOLVED 2026-06-23 — RETIRED.** `validateAIResponse({content})` + `parseSSELine` had no production caller and no genuine retarget home, so both were removed (fns + exports + their test blocks). The file, its shared `ValidationResult` typedef, the 3 live validators (`validateQuizGenerate`/`Grade`/`CourseOutline`), and the `jest.config.js` 100% pin are all kept — `validateAIResponse.js` still reports 100/100/100/100; 482 tests pass, lint clean. *Source:* code-gov audit 2026-06-15; retired 2026-06-23.

- ✅ **ERR — Standard error envelope not used in the Nuxt AI middleware. DONE 2026-06-15 [P1 #2].** `server/utils/sendError.js` emits the spec envelope `{success:false,error:{code,message},timestamp}` (tested) and the Restify routes use it, but the `server-middleware/` handlers returned bare-string errors. **Done:** the 5 quiz-generate/grade error responses in `course.js` now use the shared `sendError` envelope (codes `QUIZ_GENERATE_FAILED` / `QUIZ_GRADE_FAILED` / `PARAMS_REQUIRED`); the rest of the middleware already used it. Frontend unaffected. 302 tests pass. *Source:* code-gov audit 2026-06-15.

- ✅ **STACK DRIFT — Nuxt version — DONE on branch `chore/stack-reconciliation` (2026-06-12).** `package.json` now pins **`nuxt: "2.14.0"`** (down from the drifted 2.18.1) and installs clean on Node 14.15. Merged to `master`. *Source:* governance reconciliation 2026-06-09.

- ✅ **STACK DRIFT — Restify 11 vs Node 14.15 — DONE on branch `chore/stack-reconciliation` (2026-06-12).** `package.json` now pins **`restify: "9.1.0"`** (the Node-14-compatible line, down from the drifted `^11.1.0`). Installs clean on Node 14.15; 272/272 tests pass. Merged to `master`. *Source:* governance reconciliation 2026-06-09.

- ✅ **STACK DRIFT — `engines` pin — DONE on branch `chore/stack-reconciliation` (2026-06-12).** `package.json` now has **`engines: { node: "14.15.x" }`** and `.npmrc` carries the Node-target intent. **NOTE:** `engine-strict` is currently **`false`**, not `true` — see the open "Dev-toolchain drift past Node 14" P1 in `ACTIONS.md`. Merged to `master`. *Source:* governance reconciliation 2026-06-09.

- ✅ **RESOLVED 2026-06-15 by coding-team ruling — `openai ^4.x` ⊥ Node 14.15.** The team ruled: **keep Node 14.15 (Req 9 unchanged); do NOT use the OpenAI SDK; integrate with OpenAI by calling its REST API directly from the Restify backend; all OpenAI logic + API keys backend-only.** This **formally amends Req 7** (done in `CLAUDE.md`). **Reaffirmed by the head-team note of 2026-06-21.** Background: no version inside `^4.x` runs on 14.15. *Source:* coding-team ruling relayed 2026-06-15.

- ✅ **ARCH/BUILD [P1 ★] — Migrate OpenAI off the SDK → direct REST from Restify (implements the 2026-06-15 ruling).** **Stage 1** (`abf45c8`) — `server/utils/openaiClient.js`, a Node-14 `https` REST client (drop-in for the SDK surface, SSE-aware, 7 tests); `new OpenAI()`→`createOpenAIClient()` in both engines; `openai` dependency removed. **Stage 2** (`72d9e5c`) — engines moved to `server/advisorEngine.js` + `server/courseEngine.js`, mounted on Restify; Nuxt `server-middleware/advisor.js`+`course.js` are now thin SSE proxies; `rateLimit`→`server/utils/`; tests repointed. **STAGE (b) LIVE RUN — DONE 2026-06-16.** End-to-end verified with a real key: backend on Node **14.15** + Nuxt on Node 20. Confirmed on the locked runtime: TLS/auth/connect to `api.openai.com`; non-stream JSON success + the `validateQuizGrade` validator; SSE streaming success; two-hop SSE through the Nuxt proxy. **Fix during live run:** course-design hit OpenAI 429 because `courseEngine.js:148` injected **all 278** content summaries unfiltered — capped to the 12 most query-relevant (`filterSummariesByQuery(allUserText, 12)`). Earlier Restify-9 boot crash also fixed (`courseEngine` exported as `async (req,res,next)` which Restify 9 forbids → dropped `next`). 314/314 tests pass; all OpenAI logic + key are backend-only now. This CLOSED the P2 "ARCH — engine + OpenAI SDK in Nuxt" item. NOTE: the only remaining tail — pin `cross-env`→7 + `concurrently`→7 so `npm run dev:all` launches on 14.15 — rides the dev-toolchain reconcile (still open in `ACTIONS.md`). *Source:* coding-team ruling 2026-06-15; live verification 2026-06-16.

- ✅ **RESOLVED 2026-06-12 — `isomorphic-dompurify` vs Node 14.15 contradiction — team ruled.** Req 9 locks **Node 14.15** and the Security standard locks the sanitiser **`isomorphic-dompurify` by name**, but `^3.9.0` (and all 2.x/3.x, 1.11+) need **Node ≥18** via `jsdom`. **The coding team ruled (2026-06-11): pin to a lower compatible version, `1.3.0`** — keeps both locked clauses. Verified Node-14-safe: `1.3.0` → `jsdom@^21.1.1` (`node>=14`) and still ships **DOMPurify 3.x**. Pinned **exact `1.3.0`** (no caret). 272/272 tests pass. *Source:* coding-team ruling 2026-06-11; applied 2026-06-12.

- ✅ **SEC — Critical audit-gate allowlist — DONE 2026-06-12 (the proper "Option 1" fix).** Reconciling to locked **Nuxt 2.14.0** reintroduced **3 critical npm-audit findings** — all build-time-only and rooted in a single advisory, **`GHSA-phwq-j96m-2c2q` (`ejs` template injection, reachable only via `webpack-bundle-analyzer` at build time)**. Only fix is `nuxt@4` (forbidden). **Fix:** replaced the bare gate with **`scripts/audit-gate.js`** (Node-14, zero-dep) — blocks any *new* critical but allows only GHSA ids in an explicit `ALLOWLIST`, each cross-documented in `design/SECURITY-AUDIT-NOTES.md`. `.husky/pre-commit` now calls it. Verified: exits 0 (1 accepted) and a non-listed critical still exits 1. *Source:* pre-commit gate during 2026-06-12 reconciliation.

- ✅ **SEC — 2 critical npm-audit findings in dev tooling (`shell-quote` via `concurrently`) — RESOLVED 2026-06-10.** `shell-quote ≤1.8.3` (GHSA-w7jw-789q-3m8p) pulled in transitively by `concurrently ^9.2.1` (a devDependency). Build/dev-time only. **RESOLVED via `overrides: { "shell-quote": "1.8.4" }`** in `package.json` (non-breaking patch — `concurrently` untouched); `npm audit` criticals now **0**. *Source:* pre-commit audit gate 2026-06-09; resolved 2026-06-10.

- ✅ **SEC — IDOR: `/api/activity/*` routes — DONE 2026-06-09.** All three activity routes (`log-course`, `progression`, `team`) now sit behind `firmAuth`; advisor + firm are derived from the verified JWT, never the client. `team` additionally requires `requireManagerRole`. New test `tests/unit/activity.routes.test.js` (6 tests) proves a spoofed ID is ignored.

- ✅ **SEC — IDOR: `utils/cases.js` — CLOSED 2026-06-19 on `feat/case-study-db`.** Cases moved off client localStorage into the DB behind the secured `/api/cases` routes; `advisorId`/`firmId` are derived from the verified JWT (`firmAuth`), never the request body — reads scoped to own + firm-shared, mutations owner-only. New `tests/unit/cases.routes.test.js` proves a forged id is ignored and a non-owned row returns 404.

- ✅ **EXPERIENCE — Domain confirmation must be CAUSE-first, not problem-first. DONE (verified on `master` 2026-06-23; notes were stale).** At the `domainConfirmed` step the intake used to reflect back only the surface **defined problem / domain label** and drop the **CAUSE** the advisor described one question earlier. **Fix shipped:** `buildDomainConfirmationMessage` (`advisorEngine.js`) reflects the **driver + its knock-on effect first**, *then* names the same detected area, *then* asks the advisor to confirm/correct the driver — with a Phase-2 uncertainty dig-in, signal-anchored reflection, and a deterministic fallback if the AI call fails. On `master` (commit `72d9e5c`); **37 tests pass**. **Verified 2026-06-23.** *Source:* Mike café-test review 2026-06-10; memory `design-cause-first-not-problem-first`.

- ✅ **WIRE — The 3 proprietary frameworks, Phase 2** (read the JSON live). Phase 1 (extract to JSON) was done; this wired the live code to **read** it:
  - ✅ **Growth Fundamentals** — DONE 2026-06-09. On-screen selector (`VirtualAdvisor.vue growthStages`) imports `growth-fundamentals.json`; detector (`growth.js conversationHasGrowthStage`) reads stage names from the same file; hard-coded `GROWTH_STAGE_NAMES` removed. Single source of truth.
  - ✅ **Advisory Staircase** — DONE 2026-06-09. Selector (`VirtualAdvisor.vue staircaseSteps`) imports `advisory-staircase.json`; ceiling logic (`caseState.js staircaseToCeiling`) reads `complexityCeiling` from the same file.
  - ✅ **3 Engagement Types** — DONE 2026-06-09. **Option C satisfied:** the `DOMAIN_NATURAL_ENGAGEMENT` map relocated to a per-domain `engagementType` field on all 22 domains in `domains.json`; `caseState.js` builds the map from that file; `strategyResolver.js` reads the unknown-domain fallback from `engagement-types.json` `defaultEngagement`. Behaviour-preserving (174/174 tests pass).
  - 🚫 **Framework CONTENT is NOT firm-editable. DECISION (Mike, 2026-06-11, restated).** The proprietary *content* of all three frameworks is **platform-locked IP, off limits to firm editing.** They stay wired to their single-source JSON. **Do not re-propose making the framework content editable.**
    - ✅ **Advisory Staircase complexity-grading edit-target — DONE 2026-06-09, CORRECT, leave the code alone.** This does **not** edit framework content — it lets a firm tune the **complexity grading (the complexity ceiling) attached to each level**. Shipped: validated `GET`/`POST /api/firm-manager/staircase` (`firmManager.js`), the per-request override blend in `advisor.js:707-712`, the editor tab in `FirmManagerHub.vue` ("AI Admin" hub). **Nothing to revert.**
    - 🚫 **Growth Fundamentals** — content platform-locked; no firm edit surface (and none to be built).
    - 🚫 **3 Engagement Types** — content platform-locked; no firm edit surface (and none to be built).
    *Source:* memory `design-growth-locked-protected-ip`; frameworks-content-locked decision Mike 2026-06-11.

- ✅ **DOC — Promote the registry DRAFT → official. DONE 2026-06-09.** Mike read it through and approved; banner flipped DRAFT→official (v1.0), file renamed `design/virt-advisor-registry-DRAFT.md` → `design/virt-advisor-registry.md` (git mv, history preserved), the 3 in-repo references + archive note updated. *Source:* registry Outstanding item 7.

---

## Completed — P2 items

- ✅ **SEC — Prompt-injection: fence untrusted input. DONE 2026-06-15 [P2 #1, focused].** Raw user input was concatenated into prompt strings without delimiters: `advisor.js:74`, `course.js:408-410`. **Done (focused fencing):** new `server/utils/promptSafety.js` `fenceUntrusted()` (guard line + explicit delimiters + marker-strip break-out guard, tested). Applied at the 5 verbatim-untrusted embeds — advisor profile context, the collected-answers block in `situationBrief`, the distinction-classify `advisorText`, the course-revision `query`, and the quiz-grade `answer`. Deliberately NOT fenced where input is used only for keyword matching. 307 tests pass. **Deferred:** full per-field delimiting across the rest of the context machinery — bundle with the larger advisor refactor. *Source:* code-gov audit 2026-06-15.

- ✅ **ARCH — Advisor/course engine + OpenAI SDK moved out of Nuxt `server-middleware/` to the Restify backend. DONE (verified 2026-06-22).** Closed by the completed P1 OpenAI SDK→REST migration: the engines now live in `server/advisorEngine.js` / `server/courseEngine.js`, and `server-middleware/advisor.js` (66 lines) / `course.js` (60 lines) are true thin SSE proxies. The architecture boundary now passes (confirmed by the 2026-06-21 docs-vs-as-built audit, re-verified 2026-06-22). *Source:* code-gov audit 2026-06-15; closed by the SDK→REST migration.

- ✅ **Distinctions cascade — Stage 0 — advisor session learns its firmId — DONE 2026-06-16 (commit `7772515`).** `pages/advisor.vue` now resolves firmId in `mounted()` (SSR-safe): URL `?firmId=` override → localStorage `advisor_e_firm_id` → localhost dev fallback `dev-firm-001`. **Closed the Q1 bug end-to-end.** ✅ **PRODUCTION AUTH GAP CLOSED 2026-06-16:** `/api/advisor/query` now sits behind `firmAuth`; `firmId`/`advisorId` derived from the verified JWT and `advisorEngine.handleQuery` ignores any IDs in the body — the IDOR is closed. Verified: `tests/unit/advisor.auth.test.js` (2 tests) + full suite 319 pass + live. Residual is operational only (integration team wires the real login token/secret).

- ✅ **Distinctions cascade — Stages 1–5 — DONE 2026-06-17 (branch `feat/distinctions-cascade`).** Stable `pd-N` IDs (`ccaca96`), effective-list resolver — override replaces / decline wins, no double-boost (`2003b79`), engine wired to it (`61b2595`), firm-manager edit/reset/decline/state routes (`9ec67a4`), unified badged editable UI (`a0ddefd`). **Plus MOVE-to-domain** (backend `c7249e1`, UI `f384386`, re-move guard `34e9b2a`) and the **cross-domain near-miss bridge** (`7d57549`). 375 tests, live-verified on Node 14.15. Merged to `master` (`a1c7635`).

- ✅ **★ template inclusion-field semantics vs the engine filter — RESOLVED 2026-06-18.** The advisor engine was hard-filtering its candidate pool to `includedInClient === true`, but that field only governs whether a **client** self-serving in Advisor-e can see a template — NOT whether an advisor may recommend it *with* a client. **Fix (engine-side only):** removed the `includedInClient` gate from the selection path (`templateResolver.js:173`) and the copy path (`summaries.js:40`). Recommendable library widens **131 → ~208** `do-the-job` templates. 375 tests pass; committed `9884bd6`. *Source:* Mike's EOY test 2026-06-17; SESSION-2026-06-17-NOTES §9.

- ✅ **Distinctions can target a revenue-model GROUP** (`@rf-industry` / `@rf-general`), auto-matched to the client's industry — firms no longer have to name one industry model. Built end-to-end (resolver + firm-manager picker), tested (2026-06-18).

- ✅ **28 dormant trees — Valuation — verdict MET + NOW WIRED LIVE 2026-06-23.** Sell/exit case leads with the Sale-side assessment tools (soft hint breaks the signal-blind sell-vs-buy tie). A real production result via the soft-hint mechanism — the harness verdict asserts the wired resolver (`treeHintNames` option). The engine promotes two further Sale-side tools (EBITDA, Business Sale Assessment 1) over a buy-side tool.

- ✅ **28 dormant trees — MECHANISM — logic-tree soft hint wired into the live resolver — DONE 2026-06-23.** The "+3 soft hint" the harness modelled is now a real engine mechanism. `server/advisorEngine.js` detects the conversation's content tree(s) and walks each (`walkLogicTree`; learn-mode trees excluded) to the templates it points at; those names pass as a new `treeHintNames` option to `resolveTemplates`, which applies a weak **+3** (`tree_hint:+3`, owned constant `TREE_HINT_BOOST`) through both resolver passes. Calibrated too weak to overrule a strong industry (+8) or semantic match. 487 tests pass, lint clean, `selectionHarness` regression net clean. *Source:* harness 2026-06-23; Mike-approved build 2026-06-23.

- ✅ **28 dormant trees — Governance readiness SIGNAL `governance_too_early` — DONE 2026-06-23 (Option A).** First real "harvest judgment into a signal" build. The governance tree's gate ("too early for governance — fix foundational management first") is now a live signal: new `governance_too_early` entry in `signal-dictionary.json` (patterns lifted verbatim from the tree's gate; scoped to `governance`), and **People vs. Process** carries it via its `reviewed_signal_map` (strength 4) in `content-summaries.json` (rebuilt into `semantic-profiles.json`). Result: when the business is unready, the **engine surfaces People vs. Process at the TOP** (score 9 vs board tools 6–7.5) with no tree hint. 487 tests pass. Option B (also hold BACK formal governance tools when unready) remains a deliberate later step (tracked open in `ACTIONS.md`). *Source:* harness 2026-06-23; Mike confirmed Option A.

- ✅ **28 dormant trees — NAME ROT DISPROVEN 2026-06-23 — no name reconciliation needed.** Traced all **93** unique template names referenced across the content trees against the full `templates.json` title list: **93/93 are real, current titles — 0 stale, 0 typos, 0 renames.** The earlier "valuation name mismatch" was **false**. **One small real finding:** 20 of 93 references point at `get-organised`(18)/`get-the-job`(2) templates — 15 are the firm tree `fm_coach_culture`. *Source:* `scripts/reconcileNames.js` trace 2026-06-23.

- ✅ **BUILD — "Get the Job" trees wired into Learn mode — DONE 2026-06-23 (Stage 1).** ⚠ **These were mis-counted as "empty shells" by every audit — they are NOT empty.** Two root causes: (1) **a second, undocumented tree schema** — `type: "flat_if_then"` stores rules in a top-level `branches[]` array, NOT `nodes[]`, and **no code had ever read it**; (2) the 7 Get-the-Job trees were never tagged `mode:'learn'`. **Fix:** `formatFlatBranch` in `logicTrees.js` now renders the flat schema; the 7 trees tagged `mode:'learn'`. **Boundary fix (Mike, critical):** Get-the-Job "sales/marketing/pricing" = the **advisor selling THEIR consultancy** — the OPPOSITE of the client-mode meaning. New `isClientDeliveryLearnTree()` predicate gates the client/discover **deep-dive** to client-delivery trees only — this also **closed a pre-existing leak** (`sales_process` + `public_speaking`). 505 tests pass (`learnReferenceWiring.test.js`), lint clean. *Source:* Get-the-Job discovery + Mike's category-error catch, 2026-06-23.

- ✅ **UX — Learn-mode "wrong section" handling (reverse of the invisible swap) — DONE 2026-06-23.** An advisor picked "I'm interested in learning more" but described a **live client situation** (cafe sell + valuation); the system stayed in Learn framing. **Fix (Mike-directed):** recognise it, state it transparently, continue **in place** and expand into the how-to. Shipped at the **prompt** level (`data/prompts/learn.txt`). Live-verified. Scope = Learn mode. *Source:* live test 2026-06-23; memory `feedback_wording`.

- ✅ **★ FEATURE — Case-study feedback loop (the firm's team-development engine). MERGED to `master` (`b0afe29`, 2026-06-22).** Decision LOCKED 2026-06-17 (memory `design-case-study-review-team-dev`): advisors SEE the reasoning; **only the firm MANAGER may move distinctions.**
  - ✅ **(a) Shared case-study DB migration** (localStorage → MySQL) — **MERGED to `master` (`b786b1e`)**. `caseStore.js` + secured `/api/cases` routes (closed the `cases.js` IDOR); private/shared model + two-way visibility toggle; one-time browser→DB migration. **Still pending (ops, not code, tracked open in `ACTIONS.md`):** provision the `va_case_studies` MySQL table.
  - ✅ **(b) Manager case-review area — BUILT 2026-06-22 (`1a806f2`).** New "Team Case Studies" tab in the Firm Manager hub lists the firm's shared cases; each expands to the persisted decision trace + the advisor's post-delivery review. Backed by manager-gated `GET /api/firm-manager/cases` and a new `decision_trace` column (`10bc666`).
  - ✅ **(c) One-click "Move it here" (manager-only) — BUILT 2026-06-22 (`5cc490b`).** Each near-miss row → relocates the distinction into the case's detected area, routed by cascade source via the pure `utils/distinctionMove.js` helper. Merge gate met (populated live browser click-through 2026-06-22); merged to `master` (`b0afe29`).

---

## Completed — P3 items

- ✅ **BUILD — Intervention Urgency. DONE 2026-06-23.** `caseState.client.urgency` was computed but unused. Now wired: urgency is a named **non-overridable** Stage-3 output (`strategyResolver.js`), and on `high` a tested `urgencyDirective` (`advisorEngine.js`, exported) injects a directive into the Phase-3 recommendation prompt. **Design decisions (Mike, 2026-06-23):** (1) **template count UNCHANGED**; (2) effect = **lead with the single most critical move + flag time-pressure**; (3) the flag is **AI-phrased naturally**; plus an anti-fabrication guardrail. `sequencingRule` was found inert and `strategyResolver.templateBudget` shadowed by the meeting-count budget — hence the prompt-level approach. **15-test suite** (`tests/unit/interventionUrgency.test.js`); 510/510 pass, lint clean. Backend prompt only. *Source:* registry Stage 3; build 2026-06-23. **(Follow-up "broaden urgency-trigger detection" remains open in `ACTIONS.md`.)**

- ✅ **EDIT — Fin-Mgt Theme reconcile — DONE 2026-06-11.** `VirtualAdvisor.vue` now reads `finMgtThemes` from `fin-mgt-table.json` (single source); the hard-coded array is gone. Found 2 drifted descriptions; wiring to the data file restored the fuller wording (Mike chose the fuller version). Selection behaviour unchanged. *Source:* registry Stage 1.

- ✅ **CONTENT — Un-extracted support docs — VERIFIED EXTRACTED 2026-06-11 (item was stale).** All five named docs are extracted to JSON, on both their logic and support sides: **3 Pill Financial Management**, **Cash Tactics**, **Client Planning**, **Lite Feasibility**, **People Power**. Method/tool support content correctly lives folded into its parent domain-support file. *Source:* registry Part 2A inventory; investigation 2026-06-11.

- ✅ **QUALITY — ESLint errors cleared + pre-commit lint RE-ENABLED — DONE 2026-06-12.** The full stack-reconciliation install restored eslint. The logged "4 errors" had grown to **44** while lint was off. Cleared all 44: 38 mechanical autofixes + hand fixes (2× cosmetic `curly` braces in the **locked** markdown-pipeline files — Mike-approved, brace-only; 2× `no-irregular-whitespace` in `FirmManagerHub.vue`; 1× `require-await`; 3× `no-unused-vars` in `advisor.js` — traced first). `.husky/pre-commit` now runs eslint again. Lint: **0 errors**, 79 `no-console`-class warnings. *Source:* lint restoration, 2026-06-12.

- ✅ **CLEANUP — 3 suspected-dead exports. DONE 2026-06-23 (traced each first).** **`getSummaryByPage` + `getTemplateByPage`** (`templateRegistry.js`) — purposeless orphans, **removed**. **`SCORING_VERSION`** (`templateResolver.js`) — not cruft but an *unfulfilled intent*; **kept and wired** into the `[va-session]` log AND the persisted **decision trace** (`advisorEngine.js`), so every saved case is traceable to the engine version that produced it — part-delivering the auditability "tag each saved case with the active version" goal. +3 tests (`scoringVersionTraceability.test.js`); 513/513 pass, lint clean. *Source:* handover audit #4, 2026-06-21; resolved 2026-06-23.

---

## Recently completed (2026-06-22) — handover hardening (all on `master`, pushed)
- ✅ **Manager case-review area (case-study feedback loop b+c) MERGED** to `master` (`b0afe29`) — verified live incl. a populated browser click-through. The whole loop (a)+(b)+(c) is now on master; only `va_case_studies` table provisioning is left (ops/deploy).
- ✅ **Course-outline AI-validation gap closed** (`303c8f9`) — new `validateCourseOutline` (100% coverage) + 18 tests; a wrong-shape outline now degrades to the safe "no outline" state. Closes the last unvalidated LLM `JSON.parse`.
- ✅ **`translate.js` Node-14 + boundary + duplication — FIXED** (`9c9fda1`). The locale-translation route called global `fetch` (Node 18+) from the Nuxt layer — two breaches plus a duplicate copy. Now: the backend route uses the built-in `https` module; the Nuxt middleware is a thin proxy; 11 tests; **proven live on Node 14.15**. Closes the last genuine code-compliance violation before handover.
- ✅ **Backlog reconciled to head** (`bfed6a8`) + a `CLAUDE.md` governance refinement (`5474991`).

## Recently completed (2026-06-17) — Advisory Distinctions cascade + case-study report half
Branch `feat/distinctions-cascade` (375 tests, lint clean; merged to master `a1c7635`). Full record: `SESSION-2026-06-17-NOTES.md`.
- ✅ **Distinctions cascade built end-to-end** — stable `pd-N` IDs (`ccaca96`); effective-list resolver (`2003b79`); engine wired via single read path `firmDistinctions.js` (`61b2595`); firm-manager edit/reset/decline/state routes (`9ec67a4`); unified badged editable UI (`a0ddefd`).
- ✅ **Move a distinction to a better domain** — backend (`c7249e1`) + "Move to…" UI (`f384386`) + re-move guard (`34e9b2a`).
- ✅ **Decision trace + "Why this recommendation" panel** (case-study loop, *report* half) — backend emit (`89d6132`) + panel (`2bfa03c`).
- ✅ **Cross-domain near-miss bridge** (`7d57549`).
- ✅ **Recommendations lean to the upper-end budget** (`cef9cc0`) — above-range items offered as *extending* (stretch); R17 genuine-fit floor kept.
- ✅ **Dev:** heap cap 8→12GB (`6019451`); `eval-cheap-module-source-map` in dev (`1717953`).
- **Decided:** case-study review = team-dev engine, manager-only move; `systems`/`data-systems` are two real domains — keep both + bridge.
- **Stack reconciliation** (`chore/stack-reconciliation`) **merged to `master`** at the start of this session.

## Recently completed (2026-06-15) — source-code stack-drift sweep
A full source-level drift review (beyond `package.json`) found and **fixed same-pass** four deviations the manifest didn't surface:
- ✅ **STACK-COMPLIANCE — `nuxt.config.js` Node guard flipped to defend Node 14.15.** The guard *mandated Node 20* — directly contradicting Req 9. Now mirrors the backend guard (reject ≥22 for the spdy break; warn if `!== 14`; point to `nvm use 14.15.0`).
- ✅ **STACK-COMPLIANCE — `pages/firm-manager.vue` converted HTML→Pug (Req 5).** Was the lone remaining plain-`<template>` file. Behaviour-preserving template-only rewrite; lint clean.
- ✅ **STACK-COMPLIANCE — port aligned to spec 3000.** `nuxt.config.js` bound Nuxt to **4001** vs the Constitution's "(port 3000)". Set Nuxt to **3000** and updated `dev.bat`, `serve.bat`, and the two `restify-server.js` comments. **Dev URL is now `http://localhost:3000`.**
- ✅ **STACK-COMPLIANCE — `public/` → `static/` (Nuxt 2 convention).** `favicon.ico` + `robots.txt` lived in `public/` (a Nuxt **3** convention). `git mv`-d both into `static/`.

## Recently completed (2026-06-09)
✅ Closed IDOR on `/api/activity/*` (JWT-derived identity + manager-role gate + tests) · ✅ Activity routes brought to standard (shared `sendError` envelope + JSDoc) · ✅ HANDOFF updated · ✅ Stack-drift reconciliation plan written (`STACK-RECONCILIATION-PLAN.md`) · ✅ Registry promoted DRAFT→official **v1.0** · ✅ Logged pre-existing ESLint errors as a P3 task · ✅ **Advisory Staircase edit-target — backend** (validated firm-editable routes + runtime override-merge; +17 tests, 197 pass) · ✅ **Advisory Staircase edit-target — UI** (structured no-code editor tab in `FirmManagerHub.vue`) · ✅ **STACK-COMPLIANCE — `FirmManagerHub.vue` converted HTML→Pug.**

## Recently completed (2026-06-08)
✅ Extracted Advisory Staircase + 3 Engagement Types to JSON (Phase 1) · ✅ Fixed Growth Curve selector drift · ✅ Resolved Part 2A PDF→JSON mapping · ✅ Itemised logic trees (14 active / 28 dormant) + no-silent-parking rule · ✅ Documented all 7 non-Client functions (Part 1A) · ✅ Elevated the IDOR gap in HANDOFF · ✅ Registry final read-through + plain-language pass.
