# Virt Advisor — Action Backlog

> **The single prioritised list of every open task.** Triage from here — don't re-read the whole registry to find work.
>
> **Governance rule (no silent parking, applied to tasks):** no item may be deferred in an inline note anywhere (registry / HANDOFF / code) **without also adding a line here**. The note and the backlog line are created together. See memory `feedback-no-silent-parking`.
>
> **Stack-deviation rule (binding, see `CLAUDE.md` → Stack Constitution):** any variance from the team's locked stack spec — a dependency version bump, a new plugin, a framework variation — is logged here as a **P1 (critical) reconcile task the moment it is found or introduced**. Drift is never silently accepted as the new normal.
>
> **Legend** — Status: ☐ open · ◐ in progress · ✅ done. Type: **SEC** security · **WIRE** framework wiring · **BUILD** · **DECISION** (needs Mike) · **EDIT-TARGET** (bring a building block under Firm-Manager no-code editing) · **DOC**.
>
> **Last swept:** 2026-06-11.
>
> **★★ IP DEPTH AUDIT — DONE + FOLD-INS EXECUTED 2026-06-20 (NOT committed yet).** Full audit ran
> (pdftotext extract → depth-ratio screen → corpus-level named-concept coverage → read-verify).
> **Findings + the verified genuinely-missing list + the executed fold-ins are in
> `design/SESSION-2026-06-20-IP-DEPTH-AUDIT-NOTES.md`.** Headline: coverage broad, depth mostly
> sound; gaps were short + modest + clustered in DORMANT specialist trees. A methodology trap was
> caught (IP folded into a SIBLING file looked "missing" — People Power lives in `staff_performance`,
> get_seminar methods in `powerful-seminars.json`); the screen now checks the whole corpus.
> **8 fold-ins applied (transcribed faithfully from the source PDFs, no invented IP); 1 LIVE
> (ratio_analysis named ratios + a formatter block in `logicTrees.js`), the rest dormant-and-ready.
> dashboard_discussions verified NO change needed. 421/421 tests pass, lint clean, all JSON valid.**
> Files touched: `data/ratio-analysis-reference.json`, `get-seminar-`, `raising-capital-`,
> `fm-coach-culture-`, `valuation-`, `org-leadership-`, `org-board-pack-`, `org-firm-strategy-domain-support.json`,
> + `server/utils/logicTrees.js`. **Dormant fold-ins only reach the AI when those trees go live (see
> the 28-dormant-trees DECISION, P2).** Branch `feat/win-work-redirect` (pushed, NOT merged) still
> carries the win-work→Learn redirect + EOY upgrade work; `feat/case-study-db` also pushed + unmerged.
>
> **✅ STACK-RECONCILIATION INSTALL — UNBLOCKED 2026-06-12.** The branch now installs to **exit 0** and the full suite passes **(272/272) on Node 14.15**. Resolution chain:
> 1. **`isomorphic-dompurify` contradiction — RULED ON by the coding team (2026-06-11):** pin to the Node-14-compatible **`1.3.0`** (keeps the spec-named package *and* Node 14.15; `1.3.0`'s `jsdom@21` allows `node>=14`, and it still ships a modern DOMPurify 3.x). Exact pin, no caret (`1.11+` needs Node ≥18).
> 2. **Avast antivirus TLS interception** broke `npm install` (`UNABLE_TO_VERIFY_LEAF_SIGNATURE`) — Avast's "Web/Mail Shield" re-signs HTTPS and Node doesn't trust its root. Fixed by exporting the Avast root CA to `C:\Users\<user>\avast-root-ca.pem` and setting **`NODE_EXTRA_CA_CERTS`** to it for installs (additive; nothing weakened — same cert Windows already trusts).
> 3. **`formidable` pinned `^3.5.4 → 2.1.2`** — v3 pulls `@paralleldrive/cuid2 → @noble/hashes@1.8.0` which wants Node 14.21.3; `2.1.2` uses `hexoid` (no such chain) and runs on 14.15. Required a small callback→promise wrapper (`parseForm`) in [server/routes/firmManager.js](../server/routes/firmManager.js) since v2's `parse()` is callback-style. File-object prop names are identical v2.1↔v3, so the upload/DB code is unaffected.
> 4. **`@nuxt/friendly-errors-webpack-plugin` pinned to `2.5.2`** via `overrides` (its `2.6.0` wants Node 14.18). **`overrides` only take effect under npm 8** (which itself runs on Node 14.15) — the bundled **npm 6 ignores `overrides`** — so installs on this branch use a local npm 8 (`C:\Users\<user>\npm8tmp`). The committed **`package-lock.json` (lockfileVersion 2)** encodes the pinned tree, so reinstalls reproduce it.
> 5. **`engine-strict` relaxed `true → false`** in `.npmrc` — see the new **P1 "Dev-toolchain drift past Node 14"** below. The app RUNTIME is Node-14.15-clean (all runtime deps install with no engine warnings); the only mismatches are ~47 dev/build tools. Node 14.15 + every runtime dep stay locked.
>
> 6. **Live boot test (2026-06-12)** caught two runtime libraries the install/tests missed (both use `node:`-prefixed requires, a Node ≥14.18 feature; jest masked it because jest's own module loader handles the prefix): **`googleapis` pinned `^144.0.0 → 123.0.0`** (the last release on `google-auth-library` v8 — v124+ moved to GAL v9 → `gcp-metadata` 6 → `google-logging-utils` → `require('node:events')` crash; Drive usage in `driveService.js` is basic files/folders CRUD, identical API, no code changes) and **`mysql2` pinned `^3.22.3 → 3.16.0`** (the last clean release — 3.17.0 added `sql-escaper` which uses `require('node:buffer')`; 3.20+ also has its own `node:` usage in `lib/tracing.js`). **Result: the Restify backend BOOTS and SERVES on Node 14.15** (listening :4000; protected route returns the standard 401 envelope). A full runtime `node:`-require sweep found the only remaining runtime offender is `openai` — see the ⛔ referral below.
>
> **Install command (this branch):** `NODE_EXTRA_CA_CERTS=C:\Users\<user>\avast-root-ca.pem  <node-14.15>\node.exe  <npm8>\node_modules\npm\bin\npm-cli.js  install`
>
> **✅ RESOLVED 2026-06-15 (was the 2026-06-12 `openai ^4.x` ⊥ Node 14.15 referral): coding team ruled** — keep Node 14.15, drop the OpenAI SDK, call the OpenAI REST API directly from the Restify backend (keys backend-only). Req 7 amended in `CLAUDE.md`; the migration is a P1 build task below. Separately, the **dev-server boot is now unblocked on Node 20** via telemetry-off + `@babel/preset-env@7.10.4` (commit `5d39966`) — `npm run dev` serves on :3000 for live testing.
>
> **▶ NEXT SESSION — START HERE (updated 2026-06-18):** **All of the distinctions cascade + a full template-selection-engine repair are now MERGED TO `master`** (`a1c7635`, 392 tests, lint clean, `nuxt build` clean). Full read-cold record of today: [`SESSION-2026-06-18-NOTES.md`](SESSION-2026-06-18-NOTES.md) (selection-engine repair + the new regression harness + revenue-model group targets); prior day: [`SESSION-2026-06-17-NOTES.md`](SESSION-2026-06-17-NOTES.md) (cascade + trace panel + near-miss bridge). **Genuinely-next work:**
> 1. **★ Case-study feedback loop (the firm's team-dev engine).** Foundation-first: (a) **shared case-study DB** (localStorage→MySQL — also closes the `cases.js` IDOR), then (b) **manager case-review area**, then (c) the **manager-only one-click "Move it here."** Decision LOCKED (memory `design-case-study-review-team-dev`): advisors SEE the reasoning; **only the firm manager moves distinctions** (QC + the team rises to the owner's expertise). The DB is the heavy lift; it also lights up the 3 blank Firm Manager tabs (06-17 notes §5).
> 2. ~~**Merge `feat/distinctions-cascade` → `master`**~~ ✅ **DONE 2026-06-18** (`a1c7635`, clean --no-ff merge, 28 commits, no divergence).
> 3. ~~**★ template inclusion-field semantics**~~ ✅ **RESOLVED + EXTENDED 2026-06-18.** `includedInClient` only governs client self-serve visibility, not advisor-recommendability — **removed as a gate EVERYWHERE** (resolver, summaries copy, semantic-profile builder, firm-manager picker, 2 dev scripts; pool is now `do-the-job`). Plus the deeper café miss: `DOMAIN_SIGNAL_SCOPE` + purpose-fallback **single-sourced from the dictionary** (was drifted, omitted `revenue_modelling` from profit), **industry now scores** (wrong-industry held back), **profiles regenerated over all do-the-job**, dictionary catches **upsell + cost-transparency**, meeting-count reads hedged ranges. Locked behind a new **cross-domain regression harness** (`tests/unit/selectionHarness.test.js`). Notes 06-18.
> 3b. ✅ **NEW (2026-06-18) — distinctions can target a revenue-model GROUP** (`@rf-industry` / `@rf-general`), auto-matched to the client's industry — firms no longer have to name one industry model. Built end-to-end (resolver + firm-manager picker), tested.
> 4. **DECISION (Mike):** hide / make admin-only the raw-JSON **Decision Framework** tab? (the friendly Advisory Staircase + Advisory Distinctions tabs cover the real need; a domain-expert manager would never hand-edit JSON.)
> 5. **Dev note:** Nuxt 2 dev OOMs on *edit bursts* even at 12GB (fresh `npm run dev` restart clears; `build.cache` is the next lever). One seeded test distinction left in `systems` (dev-firm-001) for bridge testing — notes §6.
>
> **Older next-steps (still valid, lower priority, full detail in git + session memories):** signal-dictionary Phase B top-ups (`people-power`, firm-facing `org-*`/`fm-coach-culture`) — evidence-gated; the 28 dormant trees decision; the frustration→14 detector — evidence-gated; the stack reconciliation is DONE + merged to master.

---

## P1 — do first (security blockers + the core-principle work + the quick win)

> **The three STACK DRIFT items below have a written execution plan + runbook:** [`design/STACK-RECONCILIATION-PLAN.md`](STACK-RECONCILIATION-PLAN.md) (target versions, verified facts, end-of-day install steps, risks). Status there: PLAN — not yet executed.

> **─ Code-governance audit (2026-06-15) — Engineering-Standards findings (distinct from the Stack Constitution). Read-only sweep; fixes gated on Mike, one at a time. ─**

- ✅ **SEC/DATA — Shape-validate AI JSON output (the grade path drives course pass/fail). DONE 2026-06-15 [P1 #1].** The live AI JSON parses are guarded against *parse failure* (try/catch) but NOT against *valid-JSON-wrong-shape*: [course.js:424](../server-middleware/course.js#L424) (quiz-grade) returns the model's `passed`/`score`/`feedback` straight through with no type/range check — an advisor's course pass/fail rides on unvalidated AI output. Same class: [course.js:385](../server-middleware/course.js#L385) (quiz-generate), [advisor.js:88](../server-middleware/advisor.js#L88) (distinction-classify, partly checked via `Array.isArray`). **Important:** the purpose-built [server/utils/validateAIResponse.js](../server/utils/validateAIResponse.js) (tested, 100%-coverage-gated) validates a `{content:string}` shape that **matches none of these live shapes**, and `parseSSELine` targets SSE lines the frontend already parses inline (guarded) — so both util fns are effectively orphaned. Fix = targeted shape validators (boolean / number-in-range / non-empty-string) with safe fallbacks + tests (AI-validation = 100% per the testing rule), and retarget or retire the orphaned utils. **Done:** `validateQuizGenerate` + `validateQuizGrade` added to `validateAIResponse.js` (100% coverage retained), wired into both `course.js` quiz handlers; an invalid shape now routes to the existing fail-safe error (never auto-passes). +30 tests, full suite 302 pass. **Still open:** the orphaned `validateAIResponse({content})` / `parseSSELine` retarget-or-retire decision. *Source:* code-gov audit 2026-06-15.

- ✅ **ERR — Standard error envelope not used in the Nuxt AI middleware. DONE 2026-06-15 [P1 #2].** [server/utils/sendError.js](../server/utils/sendError.js) emits the spec envelope `{success:false,error:{code,message},timestamp}` (tested) and the Restify routes use it, but the `server-middleware/` handlers return bare-string errors — e.g. [course.js:391](../server-middleware/course.js#L391) → `{success:false, error:'Failed to…'}` (no `code`, no `timestamp`). Bring the middleware error responses to the standard shape. **Done:** the 5 quiz-generate/grade error responses in `course.js` now use the shared `sendError` envelope (codes `QUIZ_GENERATE_FAILED` / `QUIZ_GRADE_FAILED` / `PARAMS_REQUIRED`); the rest of the middleware (`advisor.js`, `translate.js`, course dispatch) already used it. Frontend unaffected (never reads `.error` for these endpoints; `jsonResponse` still serves the success paths). 302 tests pass. *Source:* code-gov audit 2026-06-15.

- ✅ **STACK DRIFT — Nuxt version — DONE on branch `chore/stack-reconciliation` (2026-06-12).** `package.json` now pins **`nuxt: "2.14.0"`** (down from the drifted 2.18.1) and installs clean on Node 14.15. Pending merge to `master`. *Source:* governance reconciliation 2026-06-09; `package.json`.

- ✅ **STACK DRIFT — Restify 11 vs Node 14.15 — DONE on branch `chore/stack-reconciliation` (2026-06-12).** `package.json` now pins **`restify: "9.1.0"`** (the Node-14-compatible line, down from the drifted `^11.1.0` that needed Node 16+). Installs clean on Node 14.15; 272/272 tests pass. Pending merge to `master`. *Source:* governance reconciliation 2026-06-09; `package.json`.

- ✅ **STACK DRIFT — `engines` pin — DONE on branch `chore/stack-reconciliation` (2026-06-12).** `package.json` now has **`engines: { node: "14.15.x" }`** and `.npmrc` carries the Node-target intent. **NOTE:** `engine-strict` is currently **`false`**, not `true` — see the new P1 "Dev-toolchain drift past Node 14" above for why (the runtime is 14.15-clean; ~47 dev/build tools would otherwise hard-fail the install). Flip back to `true` only after that dev-toolchain reconcile. Pending merge to `master`. *Source:* governance reconciliation 2026-06-09.

- ✅ **RESOLVED 2026-06-15 by coding-team ruling — `openai ^4.x` ⊥ Node 14.15.** The team ruled: **keep Node 14.15 (Req 9 unchanged); do NOT use the OpenAI SDK; integrate with OpenAI by calling its REST API directly from the Restify backend; all OpenAI logic + API keys backend-only.** This **formally amends Req 7** (done in `CLAUDE.md`). **Reaffirmed by the head-team note of 2026-06-21** (same three rulings: keep Node 14.15, drop the SDK, call OpenAI REST from the backend, keys backend-only). Background: no version inside `^4.x` runs on 14.15 (`openai@4.0.0` needs the Node-15 global `AbortController`; later 4.x ship `node:`-prefixed requires needing Node ≥14.18/16), so the SDK is off the table on the locked runtime. The ruling converts this from a blocker into the build task below. *Source:* coding-team ruling relayed 2026-06-15.

- ◐ **ARCH/BUILD [P1 ★] — Migrate OpenAI off the SDK → direct REST from Restify (implements the 2026-06-15 ruling) — STAGES 1 + 2 DONE 2026-06-15.** Today the `openai` SDK is imported and called inside the **Nuxt** layer — [server-middleware/advisor.js:12](../server-middleware/advisor.js#L12) and [server-middleware/course.js:16](../server-middleware/course.js#L16) (plus a commented stub in `server/routes/firm.js:60`) — which is both the Req 7 violation (SDK) and the architecture-boundary violation (third-party API + secret in Nuxt). **Plan:** (1) add a Node-14-compatible OpenAI REST client on the Restify backend (built-in `https`, or a CJS/Node-14 http lib — NO global `fetch`, that's Node 18+); cover both chat completions and the SSE streaming path. (2) Expose the advisor + course AI operations as Restify routes. (3) Reduce the Nuxt `server-middleware/` to a **thin proxy** that forwards to Restify (per the architecture rule) — moving the engine logic backend-side. (4) Read `OPENAI_API_KEY` only on the backend; remove it from any Nuxt-reachable path. (5) `validateAIResponse`/`fenceUntrusted` move with the logic. Remove the `openai` dependency once nothing imports it. **Large, behaviour-sensitive — needs its own plan + live verification.** This also CLOSES the P2 "ARCH — engine + OpenAI SDK in Nuxt" item. **DONE so far:** **Stage 1** (`abf45c8`) — `server/utils/openaiClient.js`, a Node-14 `https` REST client (drop-in for the SDK surface, SSE-aware, 7 tests); `new OpenAI()`→`createOpenAIClient()` in both engines; `openai` dependency removed. **Stage 2** (`72d9e5c`) — engines moved to `server/advisorEngine.js` + `server/courseEngine.js`, mounted on Restify (raw-body routes skip the global jsonBodyParser); Nuxt `server-middleware/advisor.js`+`course.js` are now thin SSE proxies (no `?.`); `rateLimit`→`server/utils/`; tests repointed. **Verified:** 314/314 tests; engines load on Node 14.15; **nuxt now boots on 14.15 with bundles compiling and NO `?.` ServerMiddleware crash** (that loader bug is fixed by the relocation). All OpenAI logic + key are backend-only now. **⚠ CORRECTION (2026-06-16): the earlier "Restify backend reaches listen on 14.15" claim was WRONG** — the live boot proved the backend crashed at mount: `courseEngine` was exported as `async (req, res, next)`, which Restify 9 forbids (a handler is either `async (req,res)` OR `(req,res,next)`, never both). The test suite never caught it because no test mounts the course route through a real Restify server. Fixed by dropping `next` (and its now-unreachable POST-only guard) → `async (req, res)`. **STAGE (b) LIVE RUN — DONE 2026-06-16.** End-to-end verified with a real key: backend on Node **14.15** (exact-path binary) + Nuxt on Node 20 (dev-dep batch still pending). Confirmed on the locked runtime: (1) TLS/auth/connect to `api.openai.com` (cert via `NODE_EXTRA_CA_CERTS`); (2) non-stream JSON success + the P1 `validateQuizGrade` validator (live quiz-grade returned a real graded result); (3) SSE streaming success (advisor `__intake__` streamed token deltas); (4) two-hop SSE through the Nuxt proxy (:3000→:4000→OpenAI→back). **Second fix during the live run:** course-design hit OpenAI 429 (req 61,110 tok vs org 30k TPM) because [courseEngine.js:148](../server/courseEngine.js#L148) injected **all 278** content summaries (~52k tok) unfiltered — the advisor engine already caps its summary injection, this one spot didn't. Capped to the 12 most query-relevant (`filterSummariesByQuery(allUserText, 12)`, ~15k tok); course-design now streams an outline. Both fixes lint-clean, 314/314 tests pass. NOTE: the backend `.env` is NOT auto-loaded by the standalone Restify process (only Nuxt auto-loads it) — for the live run the `.env` vars were exported into the backend shell; a Node-14-safe `.env` loader for `npm run backend` is a small follow-up. **REMAINING:** (a) pin `cross-env`→7 + `concurrently`→7 so `npm run dev:all` launches on 14.15 (part of the Issue 2 dev-dep batch); (b) ~~end-to-end live run~~ **DONE 2026-06-16**. *Source:* coding-team ruling 2026-06-15; live verification 2026-06-16.

- ✅ **RESOLVED 2026-06-12 — `isomorphic-dompurify` vs Node 14.15 contradiction — team ruled.** The contradiction was real: Req 9 locks **Node 14.15** and the Security standard locks the sanitiser **`isomorphic-dompurify` by name**, but `^3.9.0` (and all 2.x/3.x, 1.11+) need **Node ≥18** via `jsdom`. **The coding team ruled (2026-06-11): pin to a lower compatible version, `1.3.0`** — this keeps both locked clauses (Node 14.15 *and* the named package). Verified Node-14-safe: `1.3.0` → `jsdom@^21.1.1` (all `21.x` declare `node>=14`) and it still ships **DOMPurify 3.x** (`dompurify ^3.0.2`), so not security-stale. Pinned **exact `1.3.0`** (no caret — `1.11+` re-breaks). Install completes; 272/272 tests pass. *Source:* coding-team ruling 2026-06-11; applied 2026-06-12.

- ☐ **STACK DRIFT (dev toolchain) — ~47 dev/build tools require Node 16–22, above the locked 14.15. Logged 2026-06-12; `engine-strict` relaxed to unblock.** Surfaced when the Node 14.15 install finally ran end-to-end: the app **runtime is Node-14.15-clean** (backend + frontend libs install with zero engine warnings; **272/272 tests pass on 14.15**), but **47 distinct dev/build-time packages** declare a higher Node floor. They were never pinned during reconciliation, so they floated to recent majors that dropped Node 14. **Two tiers:** (a) *just above 14.15 (want 14.17/14.18)* — the **ESLint** family (`eslint@8.57.1`, `@eslint/*`, `eslint-plugin-vue`/`-promise`/`-unicorn`, `espree`, `vue-eslint-parser`, `eslint-scope`, `eslint-visitor-keys`) + `consola`; (b) *want Node 16/18/20/22* — the **CSS-minify chain** (`cssnano` + ~30 `postcss-*` plugins + `stylehacks`, Node 18+, only runs on production `nuxt build`), `concurrently@9` (Node 18 — the `dev:all` runner), `cross-env@10` (Node 20), `svgo@4` (16), `commander@11` (16), `node-releases` (18). **All are build/dev-time — none are in or reachable from the deployed runtime** (same risk class as the accepted Nuxt build-tool audit warnings in `SECURITY-AUDIT-NOTES.md`). **Interim action taken (Mike-approved "Path 1", 2026-06-12):** set `engine-strict=false` in `.npmrc` (warn, don't hard-fail) so dev work isn't blocked; the committed `package-lock.json` pins the tree. **Reconcile task (team):** pin each dev tool down to a Node-14.15-compatible version (e.g. `eslint 7.x`, `cssnano 5.x`, `concurrently 7.x`, `cross-env 7.x`) — a behaviour-changing batch (alters lint rules + production CSS-minify output) that needs review + a build/test pass, *then* flip `engine-strict` back to `true`. **⚠ Do NOT flip `engine-strict` back to `true` until this batch is done** — it will hard-fail the install again. *Source:* full engine scan during the 2026-06-12 Node 14.15 install. **UPDATE 2026-06-15 — this drift now BREAKS `nuxt build` (the CI gate), confirmed on the active Node 20 (not just 14.15):** two concrete build-blockers surfaced when the build was run as a pre-commit gate. (1) **`@nuxt/telemetry@1.5.0` → nested `rc9@2.1.2`** does `require('node:fs')`, which Nuxt 2's `esm` module loader (predates the `node:` protocol) cannot resolve → `Cannot find module 'node:fs'` even on Node 20. Workaround that clears it without a reinstall: disable telemetry (`telemetry: false` in `nuxt.config.js` / `NUXT_TELEMETRY_DISABLED=1`). (2) past telemetry, **`@babel/preset-env@7.29.7`** has removed the internal `lib/utils` path that **`@nuxt/babel-preset-app/src/polyfills-plugin.js`** still `require`s → `Cannot find module '@babel/preset-env/lib/utils'`, fatal webpack build error. **Net: `nuxt build` cannot currently go green without the dev-toolchain reconcile** — pin `@babel/core`/`@babel/preset-env` (and `@nuxt/telemetry`/`rc9`) down to Nuxt-2-compatible versions, then reinstall + build. This is the reviewed batch, not a mid-session change (reinstalls are overnight-only on this machine). Lint + the full Jest suite (272/272) are unaffected and pass.
  - ✅ **APPROVED 2026-06-15 by coding-team ruling (Issue 2):** proceed pinning dev deps to exact Nuxt-2.14.0 / Node-14.15-compatible versions — named: `@babel/core`, `@babel/preset-env`, `@nuxt/telemetry`/`rc9`, the **ESLint 7.x** family, **cssnano 5.x**, **concurrently 7.x**, **cross-env 7.x**.
  - ◐ **PARTIAL DONE 2026-06-15 (dev-boot subset, commit `5d39966`):** `telemetry: false` in `nuxt.config.js` (clears the rc9 `node:fs` boot crash) + `@babel/preset-env` pinned exact **`7.10.4`** (restores `lib/utils.createImport` for `@nuxt/babel-preset-app@2.14.0`). **`npm run dev` now compiles both bundles and serves on :3000 (Node 20); 307/307 tests pass.** This unblocks live testing today.
  - ☐ **REMAINING (the scheduled batch — end-of-day, reinstall).** **Exact pin list worked up + registry-verified 2026-06-16 (engine data straight from `npm view`):**

    | Package | Current (drifted) | → Pin to | Verified reason |
    |---|---|---|---|
    | `eslint` | `^8.57.1` | **`7.32.0`** | 8.x needs Node 14.17+; 7.32.0 engine `^10.12.0 \|\| >=12.0.0` (14.15 ✓) |
    | `@nuxtjs/eslint-config` | `^12.0.0` | **`6.0.1`** | v12 peer is `eslint ^8.23.0` (the thing dragging in eslint 8); v6.0.1 peer `eslint ^7.27.0` — pairs with 7.32.0 |
    | `concurrently` | `^9.2.1` | **`7.6.0`** | 9.x needs Node 18; 7.6.0 engine `^12.20 \|\| ^14.13 \|\| >=16` (14.15 ✓) |
    | `cross-env` | `^10.1.0` | **`7.0.3`** | 10.x needs Node 20; 7.0.3 engine `node >=10.14` (last release before the 10.x jump) |

    **Leave as-is — already Node-14.15-clean, proven booting:** `@babel/preset-env 7.10.4` (pinned in `5d39966`), `@babel/core ^7.29.0` (engine `>=6.9.0` — Node-safe; the dev-boot proved 7.29 + preset-env 7.10.4 works), `jest`/`babel-jest ^27.5.1` (engine literally lists `^14.15`), `husky ^8.0.3` (`>=14`), `pug`/`pug-plain-loader`.

    **⚠ CORRECTION to the earlier "`cssnano` → 5.x" target — it is WRONG for this stack.** Node_modules inspection (2026-06-16) shows **Nuxt 2.14's build pipeline runs on postcss 7.0.39** (nested under `autoprefixer`, `css-loader`, `optimize-css-assets-webpack-plugin`); the top-level `postcss@8.5.15` is present *only* because the drifted `cssnano@7` pulled it in — that split is part of the broken build. **cssnano 5 requires postcss 8**, so it cannot align with Nuxt 2.14. Correct move is **`cssnano 4.x`** (the postcss-7 line) **or — more likely — DROP our direct `cssnano` devDependency entirely**, since Nuxt 2.14 already bundles cssnano 4 via `optimize-css-assets-webpack-plugin` (its `cssnano-util-*` packages, a 4.x-only artefact, are in the tree). **Resolve empirically at reinstall** — this one needs an actual `nuxt build` to confirm; the four pins above are settled.

    **Then:** full install on Node 14.15 (`NODE_EXTRA_CA_CERTS` + npm 8 per the install command above) → re-run lint + tests + `nuxt build` → flip `engine-strict` back to `true`. **Two known review-pass risks:** (1) eslint-config v12→v6 shifts the lint ruleset (may surface new errors to clear); (2) cssnano/postcss alignment per above. Behaviour-changing → review required; reinstall is overnight-only on this machine. *Source:* pin list research 2026-06-16.

    - ✅ **EXECUTED 2026-06-16 (in-place install, `--legacy-peer-deps`, npm 8 + Node 14.15 + Avast cert).** The four pins above + `cssnano 4.1.11` applied; install completed (191 pkgs removed / 141 added). **`--legacy-peer-deps` was needed for the transition only** — a *false* peer conflict between the outgoing `@nuxtjs/eslint-config@12` (peer eslint 8) and incoming `@6.0.1` (peer eslint `^7.27`, satisfied by 7.32.0); future installs off the regenerated lockfile won't need it. **Verified:** lint **0 errors** (eslint-config v12→v6 surfaced NO new errors — review-pass risk #1 clear), **319 tests**, **`nuxt build` clean** (so **cssnano 4.1.11 is correct — the direct dep is NOT dropped**; review-pass risk #2 resolved — postcss 7 alignment confirmed), audit-gate **pass** (the only critical is the accepted build-time `ejs`; the "3 criticals" npm reports are 3 paths to that one advisory). **RESIDUAL — `engine-strict` left `false` (Mike's Option B, 2026-06-16):** the installed tree now has **exactly two** Node-engine mismatches, both transitive over-declarers — `consola@3.4.2` (wants 14.18+) and `node-releases@2.0.47` (wants 18+). Flipping `engine-strict: true` would hard-fail the install on those, so it stays false. **Remaining sub-task:** add 2 `overrides` (`consola`→2.x, `node-releases`→older) + one more install to verify, then flip `engine-strict` → `true`. ⚠ Risk: downgrading `consola` major (v3→v2) may break whichever build tool pulls it — own task, own install window. **★ Head-team note (2026-06-21) named `cssnano 5.x`; this is SUPERSEDED by the verified `4.1.11` above and NOT actioned** — cssnano 5 requires postcss 8, incompatible with Nuxt 2.14's postcss-7 pipeline, whereas `4.1.11` is the postcss-7 line and passed `nuxt build` clean. The "5.x" reads as an oversight (their own stated requirement was "compatible with Nuxt 2.14.0", which `4.1.11` meets). **Decision: keep `4.1.11`; flag the `5.x` back to the team for confirmation** (Mike, 2026-06-21).

- ✅ **SEC — Critical audit-gate allowlist — DONE 2026-06-12 (the proper "Option 1" fix).** Reconciling to locked **Nuxt 2.14.0** reintroduced **3 critical npm-audit findings** — all build-time-only and rooted in a single advisory, **`GHSA-phwq-j96m-2c2q` (`ejs` template injection, reachable only via `webpack-bundle-analyzer` at build time)**; `@nuxt/webpack` + `webpack-bundle-analyzer` are critical only because they depend on it. Only fix is `nuxt@4` (forbidden), so unfixable within the locked stack → they'd block **every** commit under the old bare `npm audit --audit-level=critical` gate. **Fix:** replaced the bare gate with **`scripts/audit-gate.js`** (Node-14, zero-dep, format-agnostic) — blocks any *new* critical but allows only GHSA ids in an explicit `ALLOWLIST`, each cross-documented in `design/SECURITY-AUDIT-NOTES.md`. `.husky/pre-commit` now calls it. Verified: exits 0 (1 accepted) and a non-listed critical still exits 1. *Source:* pre-commit gate hit during the 2026-06-12 stack-reconciliation commit.

- ✅ **SEC — 2 critical npm-audit findings in dev tooling (`shell-quote` via `concurrently`) — RESOLVED 2026-06-10.** `shell-quote ≤1.8.3` (advisory GHSA-w7jw-789q-3m8p — `quote()` does not escape newlines) is pulled in transitively by `concurrently ^9.2.1` (a **devDependency** — the `dev:all` script runner). Both are **build/dev-time only — not in, and not reachable from, the deployed runtime** (same accepted-risk category as the Nuxt build-tool warnings in `SECURITY-AUDIT-NOTES.md`). Surfaced 2026-06-09 when they **blocked a commit** — the deliberate `--audit-level=critical` pre-commit gate caught them (working as designed). Safe `npm audit fix` (no `--force`) does **not** clear them: the patched `shell-quote 1.8.4` is only reachable via a breaking `concurrently` major bump. **Remediation = its own reviewed task, not an inline hack — decide between:** (a) pin `shell-quote` to `1.8.4` via an `overrides` entry (non-breaking, but a manifest deviation to log), (b) bump `concurrently` to v10 (breaking dev-tool change), or (c) formally accept + record in `SECURITY-AUDIT-NOTES.md` alongside the other build-tool risk. **RESOLVED via option (a):** `overrides: { "shell-quote": "1.8.4" }` added to `package.json` (non-breaking patch — `concurrently` untouched); `npm audit` criticals now **0** and the pre-commit gate passes normally (no `--no-verify`). *Source:* pre-commit audit gate, 2026-06-09; resolved 2026-06-10; `npm audit`.

- ◐ **SEC — Client-supplied identity (IDOR).**
  - ✅ **`/api/activity/*` routes — DONE 2026-06-09.** All three activity routes (`log-course`, `progression`, `team`) now sit behind `firmAuth`; advisor + firm are derived from the verified JWT, never the client. `team` additionally requires `requireManagerRole` (advisor → own only; manager → own firm only). Advisor identity added to `firmAuth` as `req.advisorId` (configurable `advisorIdClaim` in `config/integration.js`, falls back to the JWT `sub` claim; matching dev-bypass advisor ID). Front-end screens now send the pass and no longer send IDs in the request. New test `tests/unit/activity.routes.test.js` (6 tests) proves a spoofed ID in the request is ignored.
  - ☐ **`utils/cases.js` — STILL OPEN.** Still trusts `advisorId`/`firmId`, but it is client-side localStorage today (no server endpoint to exploit). Its IDOR fix lands with the **Case-study DB migration (P2)**, where the data first becomes shared/server-side.
  - **Why:** as-is, anyone could read another advisor's or firm's data by changing the IDs. **Gate:** activity routes closed; cases.js gated to the DB migration. *Source:* registry Part 1A → Progression; HANDOFF Security Notes.

- ◐ **EXPERIENCE — Make the conversational intake read the advisor (delivers Principle P2).** Surfaced live 2026-06-09 in Mike's café test (battery interrogation, no read on advisor signals). **Largely delivered 2026-06-10** — battery removed (Stage 2 `5047963`), cause-first confirmation, prep-mode, and the cause-signal lever all shipped. Remaining facets below:
  - ◐ **Frustration / dead-end detection.** **The "Option 1 — three choices" model is SUPERSEDED** by the resistance→14 model (memory `design-intake-resistance-fallback`): never skip the 14 to jump to recommendations. **Dead-end / uncertainty DONE 2026-06-10** — `detectNotMetClient` (prep-mode offer) + `detectUncertainty` (uncertainty-gated dig-in). **Still NOT built:** a **frustration detector** (anger/profanity/"I already told you" → acknowledge + fall back to the 14) — the original "profanity sailed past" failure.
  - ✅ **Prep-mode awareness — DONE 2026-06-10 (`025e6a1`).** `detectNotMetClient` offers to skip the client-about questions; skipped fields get a `'skipped'` sentinel. Tagging = client-about (skip) vs advisor/relationship (keep), per Mike.
  - ◐ **Adaptive question volume.** Battery removed; the dig-in/prep-mode trim what's asked. The cold primary-issue selector was absorbed into the cause-signal lever (the cause answer + check-in now drive selection directly). *Source:* Mike café test 2026-06-09; memory `feedback-design-philosophy`, `design-intake-resistance-fallback`.

- ☐ **EXPERIENCE — Domain confirmation must be CAUSE-first, not problem-first.** Surfaced live 2026-06-10 (Mike, café-test review). At the `domainConfirmed` step the intake reflects back the surface **defined problem / domain label** ("I'm reading this as profitability & feasibility") but drops the **CAUSE** the advisor described one question earlier (`situationDiagnostic`: contributing factors + downstream effects). Advisors reason cause-first; confirming the label alone gets the system **off line** with the advisor — and the cause may relocate the intervention to a different domain (registry Part 7A symptom-vs-cause). **Fix direction:** the confirmation/summary reflects the driver + downstream effects first, *then* names the area. **Couples with** the primary-issue propose→confirm redesign (P2 below) — same cause-first principle, applied at two points in the flow. *Source:* Mike café-test review 2026-06-10; memory `design-cause-first-not-problem-first`, `design-primary-issue-step`.

- ☐ **WIRE — The 3 proprietary frameworks, Phase 2** (the ★ top priority — this is what delivers Principle P1 for the most valuable IP). Phase 1 (extract to JSON) is done; now wire the live code to **read** the JSON:
  - ✅ **Growth Fundamentals** — DONE 2026-06-09. On-screen selector (`VirtualAdvisor.vue growthStages`) now imports `growth-fundamentals.json`; detector (`growth.js conversationHasGrowthStage`) reads stage names from the same file; hard-coded `GROWTH_STAGE_NAMES` array removed. 3-copy duplication closed — single source of truth.
  - ✅ **Advisory Staircase** — DONE 2026-06-09. Selector (`VirtualAdvisor.vue staircaseSteps`) now imports `advisory-staircase.json` (label keeps "Step N:" prefix for the server's number-finder; descriptions use data-file `selectorDescription` wording); ceiling logic (`caseState.js staircaseToCeiling`) reads `complexityCeiling` from the same file. Steps + ceiling de-duplicated. NOTE: `education-gates-ascent` remains display-only / not-wired — separate DECISION in P2.
  - ✅ **3 Engagement Types** — DONE 2026-06-09. **Option C satisfied:** the `DOMAIN_NATURAL_ENGAGEMENT` map was relocated to a per-domain `engagementType` field on all 22 domains in `domains.json` (single source of truth); `caseState.js` now builds the map from that file; the duplicate map block was removed from `engagement-types.json` and the revisit block marked done; `strategyResolver.js` reads the unknown-domain fallback from `engagement-types.json` `defaultEngagement`. Behaviour-preserving (174/174 tests pass; engagement types resolve identically).
  - 🚫 **Framework CONTENT is NOT firm-editable. DECISION (Mike, 2026-06-11, restated).** The proprietary *content* of all three frameworks — the 3 Engagement Types, the 5-step Advisory Staircase, and the **entire** Growth Fundamentals framework (the steps/types/stages and their proprietary meaning) — is **platform-locked IP, off limits to firm editing.** They stay wired to their single-source JSON (so they drive the engine + the AI without code changes); what is ruled out is *a firm rewriting the framework itself*. The old "surface all three so a firm can edit them" goal was an assumption that accreted from generalising one early build — never Mike's call. **Do not re-propose making the framework content editable.**
    - ✅ **Advisory Staircase complexity-grading edit-target — DONE 2026-06-09, CORRECT, leave the code alone.** This does **not** edit framework content — it lets a firm tune the **complexity grading (the complexity ceiling) attached to each level**, i.e. how much complexity the firm allows at each relationship depth. That is legitimate firm tuning, fully consistent with "framework content locked." Shipped: validated `GET`/`POST /api/firm-manager/staircase` ([firmManager.js](../server/routes/firmManager.js)), the per-request override blend in [advisor.js:707-712](../server-middleware/advisor.js), the editor tab in `FirmManagerHub.vue` (the "AI Admin" hub). **Nothing to revert.**
    - 🚫 **Growth Fundamentals** — content platform-locked; no firm edit surface (and none to be built).
    - 🚫 **3 Engagement Types** — content platform-locked; no firm edit surface (and none to be built).
    *Source:* memory `design-growth-locked-protected-ip`; frameworks-content-locked decision Mike 2026-06-11 (the staircase *grading* override is the deliberate exception — it tunes grading, not content).

- ✅ **DOC — Promote the registry DRAFT → official. DONE 2026-06-09.** Mike read it through and approved; banner flipped DRAFT→official (v1.0), draft warnings removed, file renamed `design/virt-advisor-registry-DRAFT.md` → `design/virt-advisor-registry.md` (git mv, history preserved), the 3 in-repo references + archive note updated. Deliberate inline ⚠/GAP future-work flags retained by design. *Source:* registry Outstanding item 7.

---

## P2 — important, decide/build soon

> **─ Code-governance audit (2026-06-15), P2 items ─**
- ✅ **SEC — Prompt-injection: fence untrusted input. DONE 2026-06-15 [P2 #1, focused].** (was: raw user input concatenated into prompt strings without delimiters) [advisor.js:74](../server-middleware/advisor.js#L74) (`${advisorText…}`), [course.js:408-410](../server-middleware/course.js#L408-L410) (question + answer). CLAUDE.md mandates explicit delimiters; never concatenate. Mitigated (constrained tasks, temp 0, truncation, `sanitiseInput` on the advisor body at [advisor.js:662](../server-middleware/advisor.js#L662)) but the delimiter rule isn't met. **Done (focused fencing):** new `server/utils/promptSafety.js` `fenceUntrusted()` (guard line + explicit delimiters + marker-strip break-out guard, tested). Applied at the 5 verbatim-untrusted embeds — advisor profile context, the collected-answers block in `situationBrief`, the distinction-classify `advisorText`, the course-revision `query`, and the quiz-grade `answer`. Deliberately NOT fenced where input is used only for keyword matching (`buildClientContext` searchQuery — fencing would pollute matching). 307 tests pass. **Note:** prompt structure changed and the tests mock OpenAI, so a live advisor-session + quiz eyeball on Node 20 is recommended. **Deferred:** full per-field delimiting across the rest of the context machinery — bundle with the larger advisor refactor (P2). *Source:* code-gov audit 2026-06-15.
- ☐ **ARCH — Advisor/course engine + OpenAI SDK live in Nuxt `server-middleware/`, not Restify** (`advisor.js` 2061 lines, `course.js` 507) — not the "thin proxy" the spec requires. **NOW UNBLOCKED 2026-06-15:** the team's Req 7 amendment directs moving all OpenAI logic + keys to the Restify backend, which closes this item — it is folded into the **P1 "Migrate OpenAI off the SDK → direct REST from Restify"** task above. Track + do it there. *Source:* code-gov audit 2026-06-15; coding-team ruling 2026-06-15.

- ☐ **FEATURE [★ north-star] — Advisory Distinctions mentor→firm→advisor cascade.** Plan: [`design/DISTINCTIONS-CASCADE-PLAN.md`](DISTINCTIONS-CASCADE-PLAN.md). Confirmed model (Mike 2026-06-16): 3-tier mentor→firm→advisor; **opt-out** (mentor distinctions on by default); a firm can **decline** or **edit** them; **firm customisation wins and sticks** (firm closest to client = final say). Advisors use their firm's *effective list* (live mentor − firm-declined + firm-edits + firm-own). Delivers north-star commitment #1 (firm adds IP + steers). **Staged, foundation-first:**
  - ✅ **Stage 0 — advisor session learns its firmId — DONE 2026-06-16 (commit `7772515`).** [pages/advisor.vue](../pages/advisor.vue) now resolves firmId in `mounted()` (SSR-safe): URL `?firmId=` override → localStorage `advisor_e_firm_id` → localhost dev fallback `dev-firm-001`. **Closed the Q1 bug end-to-end** — verified live: the conflict/alignment session that previously dropped the alignment tool now loads the firm's "Lite Strategy" row, which scores top (10) and surfaces (budget 3 via the Q2 meeting-count fix). ✅ **PRODUCTION AUTH GAP CLOSED 2026-06-16:** `/api/advisor/query` now sits behind `firmAuth`; `firmId`/`advisorId` are derived from the verified JWT (`req.firmId`/`req.advisorId`) and `advisorEngine.handleQuery` ignores any IDs in the body — the IDOR is closed. Front-end (`VirtualAdvisor.vue` via `advisor.vue`) sends the Bearer token (real JWT when logged in; dev-bypass on localhost). Verified: `tests/unit/advisor.auth.test.js` (2 tests) + full suite 319 pass + live (no token → 401, dev-bypass → session runs). Residual is operational only — the integration team wires the real login token/secret (placeholder `JWT_SECRET` in dev; HANDOFF Step 4/5).
  - ✅ **Stages 1–5 — DONE 2026-06-17 (branch `feat/distinctions-cascade`).** Stable `pd-N` IDs (`ccaca96`), effective-list resolver — override replaces / decline wins, no double-boost (`2003b79`), engine wired to it (`61b2595`), firm-manager edit/reset/decline/state routes (`9ec67a4`), unified badged editable UI (`a0ddefd`). **Plus MOVE-to-domain** (backend `c7249e1`, UI `f384386`, re-move guard `34e9b2a`) and the **cross-domain near-miss bridge** (`7d57549`). 375 tests, live-verified on Node 14.15. *(Decline = "Switch off"; edit replaces the platform row and sticks; move relocates a distinction's whole content to a better domain.)*
  - ☐ **Stage 3 — hierarchy hook-up** to the master app (mentor→firms→advisors). ⚠ Open: is "mentor" a new role or `platform_admin` for now?
  - ☐ **Stage 4 (later) — case-study → suggested-distinction loop** (north-star #4; out of scope for the cascade build itself).
  - **Shared deps:** Stages 1–2 persistence rides the **"Firm Manager config persistence → MySQL"** P2 item below (dev-JSON fallback is test-only). *Source:* Mike 2026-06-16; memory `design-distinctions-cascade`; the Q1/Q2 investigation 2026-06-16.

- ✅ **★ template inclusion-field semantics vs the engine filter — RESOLVED 2026-06-18.** The advisor engine was hard-filtering its candidate pool to `includedInClient === true`, but that field only governs whether a **client** self-serving in Advisor-e (no advisor) can see a template — NOT whether an advisor may recommend it *with* a client. Each inclusion field governs a *different* Advisor-e surface and none belongs in advisor selection: `includedInClient` = client self-serve search; `cpd.isHidden` = CPD log widget; `growth.isHidden` = Growth framework. **Fix (engine-side only, no new field, no master-app change):** removed the `includedInClient` gate from the selection path ([templateResolver.js:173](../server/utils/templateResolver.js#L173)) and the copy path ([summaries.js:40](../server/utils/summaries.js#L40)), with why-comments at both; the `menuSection`/`subSection`/scoring layers already did the real work. Recommendable library widens **131 → ~208** `do-the-job` templates (E.O.Y Meeting, 5 Layers Questionnaire, Advisory Proposal, Client Onboarding now recommendable). 375 tests pass; committed `9884bd6` on `feat/distinctions-cascade`. *Source:* Mike's EOY test 2026-06-17; resolved 2026-06-18; SESSION-2026-06-17-NOTES §9.
- ☐ **DECISION (Mike) — The 28 dormant diagnostic trees.** 28 of your 42 decision trees drive no live path. Decide: switch on (wire into Stage 2 diagnosis), retire, or partial. **Why:** significant proprietary IP sitting idle. *Source:* registry Part 2 dormant-asset register.
- ☐ **★ FEATURE — Case-study feedback loop (the firm's team-development engine).** Decision LOCKED 2026-06-17 (memory `design-case-study-review-team-dev`): advisors SEE the reasoning (the trace, shipped); **only the firm MANAGER may move distinctions** (quality control + the team rises to the owner/manager's expertise). The *report* half is DONE (decision trace + "Why this recommendation" panel + cross-domain bridge, all on `feat/distinctions-cascade`). The *review + act* half is foundation-gated, three steps:
  - ☐ **(a) Shared case-study DB migration** (localStorage → MySQL). The foundation — today a saved case lives only on the advisor's own machine, so a manager can't review their team. **Also closes the `cases.js` IDOR.** Heavy lift; also lights up the 3 blank Firm Manager tabs (Document Library / Decision Framework / Templates & Videos — all need Drive/DB). *Source:* HANDOFF → Learning Loop; SESSION-2026-06-17-NOTES §3, §5.
  - ☐ **(b) Manager case-review area** — manager browses advisors' saved case studies, opens each one's "Why this recommendation" trace + near-misses, judges it.
  - ☐ **(c) One-click "Move it here" (manager-only)** — lives in the review area (NOT the advisor panel). Reuses today's move endpoint + firm-own domain-change. *(The advisor panel's bridge stays informational.)*
- ☐ **BUILD — Course progress persistence.** The `progress` handler is a labelled stub (`CourseReminderService.markComplete`); wire to MySQL + firm-level reporting. *Source:* registry Part 1A → Course.
- ◐ **BUILD — Primary-issue selector redesign — LARGELY OVERTAKEN (verified in code 2026-06-11).** The original job (replace the cold pick-from-a-list card with a propose→confirm step) is **substantially done by removal**: the `primaryIssue` question is permanently skipped (`skip: () => true`, [advisor.js:968-972](../server-middleware/advisor.js)) and the issue is now **inferred from the conversation** — the `problemSignals` cause-signal lever (~77% of selection, shipped 2026-06-10) + domain — and is no longer a mandatory field ([advisor.js:1378](../server-middleware/advisor.js)). **Only remnant still open:** a **recommendation-time clarification** that fires *only if* template scoring hits a genuine issue fork (the inline note at [advisor.js:962-967](../server-middleware/advisor.js)). This is **evidence-gated** — build it when a real session actually produces a fork, not preemptively (the cause lever may make forks rare). *Source:* registry Stage 2 design-debt note; memory `design-primary-issue-step`, `session-2026-06-10`.
- ☐ **BUILD (pre-production) — Firm Manager config persistence → MySQL.** The Firm Manager config **writes** (template-library import, Advisory Staircase override, Advisory Distinctions) fall back to **gitignored local JSON files** when MySQL is unavailable, so the screens can be tested on a dev machine. **These dev-file fallbacks are TEST-ONLY** (gated behind `IS_DEV`, no version history) and must be replaced by real persistence before production: a live MySQL instance + the `firm_framework_versions` table via `firmOverlay` (the schema is in `config/db-schema.sql`). **Why logged:** the fallback makes local testing pass, so without this line the missing real persistence could be silently mistaken for "done." *Source:* HANDOFF "DEV/TEST-ONLY persistence fallbacks"; added 2026-06-09 during staircase-UI upload testing.

- ☐ **BUILD — Firm Manager: master-export self-service upload (Stage 2 of the search_content plan).** Replace the developer-managed export file with a Firm Manager screen where a firm downloads the template export from Advisor-e and uploads it themselves. Per-firm Google Drive folder (`/VirtAdvisor/firms/{firmId}/`), schema-validated upload (size cap, JSON-only, shape check, last-known-good fallback), version history + restore (= auditability), firm_manager role-gated. **Blocked on** Firm Manager Auth (hub Phase 1) — no verified firmId to scope the file to until then; lands as part of hub Phase 5 (Template & Video Library). **Stage 1** (single central loader + validation, single-firm interim, file in one defined folder) ships first and this swaps in behind it via that one loader. *Source:* memory `firm-manager-hub`; 2026-06-08 master-export design discussion.

---

## P3 — improvements, editing-targets, auditability

> **─ Code-governance audit (2026-06-15), P3 items ─**
- ☐ **STRUCT — Monolithic components, no base/shared split.** `VirtualAdvisor.vue` 2708, `CourseBuilder.vue` 2152, `FirmManagerHub.vue` 1295, `FirmDashboard.vue` 665 — far over the "decompose when complex and >200 lines" rule; no `components/base/` or `components/shared/`. *Source:* code-gov audit 2026-06-15.
- ☐ **STATE (Mike) — Vuex installed but unused; no `store/` dir.** Global state lives in localStorage (profile, courses, cases). Contradicts "Vuex is the only global state mechanism." Best decided alongside the localStorage→MySQL migration (P2). *Source:* code-gov audit 2026-06-15.
- ☐ **DOC — Sparse JSDoc.** Mixins lack `@param`/`@returns`; `course.js` has none; `advisor.js` ~4 tags across 2061 lines. Rule: document mixins, middleware proxies, routes with `@route`/`@param`/`@returns`. *Source:* code-gov audit 2026-06-15.
- ☐ **I18N — Hardcoded English in templates** (e.g. "Access Restricted" in `pages/firm-manager.vue`); should route through `$t()`. Infra (`localeMixin`, 8 locales) exists; pervasive sweep. *Source:* code-gov audit 2026-06-15.

- ☐ **EDIT-TARGET — Bring building blocks under Firm-Manager no-code editing** (close the "✗ editable" column in Part 2): 14-question **weight sliders**, **Strategy table** (`strategyResolver` rules), **primary-issues** table, **content-summaries** editor, **coaching-reference** editor, **logic-trees** flowchart editor (tied to the 28-trees decision). *Source:* registry Part 2.
- ☐ **BUILD — Intervention Urgency.** `caseState.client.urgency` is computed but unused; make it a Stage-3 output that compresses sequencing + cuts template count when `high`. *Source:* registry Stage 3.
- ☐ **BUILD — Profile → DB.** Move the advisor profile off localStorage into the firm DB (same migration family as case studies). *Source:* registry Part 1A → Profile.
- ☐ **BUILD — Close the improvement loop.** Case-study → suggested-distinction flow; wire coaching-reference editing into Firm Manager. *Source:* registry Part 9.
- ☐ **BUILD — Auditability goals.** Decision Trace (per-recommendation trace of issue/lenses/scores) + Config versioning (edit history; tag each saved case with the active config version). *Source:* registry Part 9.
- ☐ **BUILD — Retire the R17 relevance gate** once the deterministic scoring matures (interim AI stopgap). *Source:* registry Stage 4.
- ☐ **DECISION (Mike) — HOW-swap scope.** Confirm whether the invisible client→learn swap should also fire in discover mode and pre-recommendation. *Source:* registry Part 8.
- ☐ **DECISION (Mike) — hide / admin-only the raw-JSON "Decision Framework" Firm Manager tab?** It lets someone hand-edit the firm's decision config as raw JSON (version history + restore). The **friendly** equivalents already exist (Advisory Staircase + Advisory Distinctions tabs), and a domain-expert manager would never edit raw JSON. Decide: keep as a power-user/admin tool (hidden from ordinary managers), or remove. *Source:* SESSION-2026-06-17-NOTES §5, §7; Mike's "I don't understand these tabs" review 2026-06-17.
- ✅ **EDIT — Fin-Mgt Theme reconcile — DONE 2026-06-11.** `VirtualAdvisor.vue` now reads `finMgtThemes` from `fin-mgt-table.json` (single source, mirrors `growthStages`/`staircaseSteps`); the hard-coded array is gone. Found 2 drifted descriptions (shortened in the component) — wiring to the data file restored the fuller wording (Mike chose the fuller version). Selection behaviour unchanged. *Source:* registry Stage 1.
- ☐ **DECISION (Mike) — Education gates on the Advisory Staircase.** Mike's rule: education is the enabler to climb the staircase — where a client lacks foundational financial literacy (Lens 2), education must be introduced (commonly Step 2) before progressing. Currently **display-only / not wired** (captured in `advisory-staircase.json` designIntent + Step 2 selectorDescription). Decide: wire it as a real Stage-3 input, or keep guidance-only. **Why logged separately:** previously only referenced inside the frameworks-wiring note, never its own job. *Source:* registry Stage 1/Stage 7A; memory `design-education-gates-ascent`.
- ☐ **EDIT-TARGET — Plan-mode's 2 proprietary frameworks** are embedded (locked) inside `plan.txt` and flagged "should become firm-editable," but were not on the EDIT-TARGET list. Add to the firm-editable consideration (or consciously decide they stay prompt-locked). *Source:* registry Part 1A → Plan ("locked-in-prompt" GAP).
- ✅ **CONTENT — Un-extracted support docs — VERIFIED EXTRACTED 2026-06-11 (item was stale).** All five named docs are extracted to JSON, on both their logic and support sides; the line predated the extraction and was never updated. Evidence: **3 Pill Financial Management** → `logic_trees.json` (`three_pill_fin_mgt`) + `fin-mgt-table.json` (themes/approach table); **Cash Tactics** → `logic_trees.json` (`cash_tactics`) + full *"Cash Tactics — Activity Ratios and Working Capital Tactics"* framework folded into `forecasting-domain-support.json`; **Client Planning** → `logic_trees.json` (`client_planning`) + full *"Client Planning Framework"* in `strategy-domain-support.json`; **Lite Feasibility** → `logic_trees.json` (Lite Feasibility tree, with `source_support` provenance) + full *"Lite Feasibility Framework"* in `profit-domain-support.json`; **People Power** → tree from `People Power template logic.pdf` + dedicated `people-power-domain-support.json`. Method/tool support content correctly lives folded into its parent domain-support file (not a standalone file), which is why a name-only search made them look absent. The separate Org-CA-Capacity-Planner mislabel stays tracked under the DOC-tidy item below. *Source:* registry Part 2A inventory; investigation 2026-06-11.
- ☐ **DOC tidy** — fold any remaining per-file detail from `registry_compilation_wip` (Part 2 note); resolve the Org CA Capacity Planner mislabelled-PDF flag (Part 2A). *Source:* registry.
- ✅ **QUALITY — ESLint errors cleared + pre-commit lint RE-ENABLED — DONE 2026-06-12.** The full stack-reconciliation install restored eslint, so lint came back on. The logged "4 errors" had grown to **44** while lint was off (new code was never scanned — the exact failure mode this line predicted). Cleared all 44: 38 mechanical autofixes (`no-useless-return` ×26, quotes/arrow-parens/import-order/spacing) + hand fixes — 2× cosmetic `curly` braces in the **locked** markdown-pipeline files (Mike-approved, brace-only, no logic), 2× `no-irregular-whitespace` in `FirmManagerHub.vue` (the deliberate nbsp spacer labels rewritten as the `' '` escape — renders identically), 1× `require-await` (OPTIONS preflight handler made non-async with explicit `next()`), 3× `no-unused-vars` in `advisor.js` (traced first: `resolveTemplates` import, `PRIMARY_ISSUES` require, `CONTEXT_DOMAINS` set — all zero-use intake-redesign leftovers; see the note below). `.husky/pre-commit` now runs eslint again (errors block; warnings pass). Lint: **0 errors**, 79 `no-console`-class warnings. *Source:* lint restoration, 2026-06-12.

- ☐ **DECISION (Mike) — "Context domains override the strategy layer": design intent has NO implementation.** Surfaced while clearing the `no-unused-vars` lint error on `CONTEXT_DOMAINS` in `advisor.js` (2026-06-12): the constant (`conflict`, `eoy`, `due-diligence`) and its comment — "Context domains have no primary issues — they override the strategy layer instead" — were **dead code with zero references**, and a repo-wide search found **no other code implementing any context-domain strategy override**. The dead constant was removed (behaviour-preserving — there was no behaviour); this line preserves the *design idea* per no-silent-parking. Decide: should context domains get a real strategy-layer override (Stage 3), or is the current per-domain handling (e.g. `engagementType` in `domains.json`, eoy category in `caseState.js`) already the intended design? *Source:* lint cleanup trace, 2026-06-12.

---

## Recently completed (2026-06-17) — Advisory Distinctions cascade + case-study report half
Branch `feat/distinctions-cascade` (375 tests, lint clean, pushed; NOT yet merged to master). Full record: [`SESSION-2026-06-17-NOTES.md`](SESSION-2026-06-17-NOTES.md).
- ✅ **Distinctions cascade built end-to-end** — stable `pd-N` IDs (`ccaca96`); effective-list resolver, override-replaces / decline-wins, no double-boost (`2003b79`); engine wired via single read path `firmDistinctions.js` (`61b2595`); firm-manager edit/reset/decline/state routes (`9ec67a4`); unified badged editable UI (`a0ddefd`).
- ✅ **Move a distinction to a better domain** — backend (`c7249e1`) + "Move to…" UI (`f384386`) + re-move guard, blocks duplicate/lost-edit (`34e9b2a`).
- ✅ **Decision trace + "Why this recommendation" panel** (case-study loop, *report* half) — backend emit (`89d6132`) + panel (`2bfa03c`).
- ✅ **Cross-domain near-miss bridge** (`7d57549`) — surfaces firm distinctions filed under the wrong domain that matched this session (informational in the advisor panel; one-click move deferred to the manager review area).
- ✅ **Recommendations lean to the upper-end budget** (`cef9cc0`) — fill the engagement; above-range items offered as *extending* (stretch); R17 genuine-fit floor kept (no padding). Verified: budget 3 → 3 cards.
- ✅ **Dev:** heap cap 8→12GB (`6019451`); `eval-cheap-module-source-map` in dev (`1717953`).
- **Decided:** case-study review = team-dev engine, manager-only move (memory `design-case-study-review-team-dev`); `systems`/`data-systems` are two real domains — keep both + bridge.
- **Stack reconciliation** (`chore/stack-reconciliation`) **merged to `master`** at the start of this session.

## Recently completed (2026-06-15) — source-code stack-drift sweep
A full source-level drift review (beyond `package.json`) found and **fixed same-pass** four deviations the manifest didn't surface:
- ✅ **STACK-COMPLIANCE — `nuxt.config.js` Node guard flipped to defend Node 14.15.** The guard *mandated Node 20* (`if (major !== 20) warn`, "Node 20 is the supported runtime") — directly contradicting Req 9 and the already-correct sibling guard in `server/restify-server.js`. Per the Constitution this is "a document found offering such an option … a defect to be corrected on sight." Now mirrors the backend guard (reject ≥22 for the spdy break; warn if `!== 14`; point to `nvm use 14.15.0`).
- ✅ **STACK-COMPLIANCE — `pages/firm-manager.vue` converted HTML→Pug (Req 5).** Was the lone remaining plain-`<template>` file (after `FirmManagerHub.vue` was done 2026-06-09). Behaviour-preserving template-only rewrite; `<script>`/`<style scoped>` untouched; lint clean.
- ✅ **STACK-COMPLIANCE — port aligned to spec 3000.** `nuxt.config.js` bound Nuxt to **4001** vs the Constitution's "(port 3000)"; repo was also internally inconsistent (`README.md` + `start-dev.bat` already said 3000, but `dev.bat`/`serve.bat`/comments said 4001). Set Nuxt to **3000** and updated `dev.bat`, `serve.bat`, and the two `restify-server.js` comments. CORS unaffected — its regex matches localhost on any port. **Dev URL is now `http://localhost:3000`.**
- ✅ **STACK-COMPLIANCE — `public/` → `static/` (Nuxt 2 convention).** `favicon.ico` + `robots.txt` lived in `public/` (a Nuxt **3** convention Nuxt 2 does not serve), so the favicon was effectively dead. `git mv`-d both into `static/`; they now serve at root as `nuxt.config.js` head expects.

**Still BLOCKED, not actioned (correctly):** the OpenAI architecture-boundary drift — `openai` SDK + the advisor engine live in the Nuxt `server-middleware/` layer (`advisor.js` 2061 lines, `course.js` 507 lines) instead of the Restify backend. This is entangled with the **⛔ referred** `openai ^4.x` vs Node 14.15 contradiction (see P1): no `^4.x` version runs on Node 14.15 regardless of where it's imported, so the AI must not action it under the one-directional rule. Left for the coding team's ruling + a scoped refactor task.

## Recently completed (2026-06-09)
✅ Closed IDOR on `/api/activity/*` (JWT-derived identity + manager-role gate + tests) · ✅ Activity routes brought to standard (shared `sendError` envelope + JSDoc) · ✅ HANDOFF updated (activity IDOR resolved; `advisorIdClaim` added) · ✅ Stack-drift reconciliation plan written (`STACK-RECONCILIATION-PLAN.md`) · ✅ Registry promoted DRAFT→official **v1.0** (renamed, refs updated) · ✅ Logged pre-existing ESLint errors as a P3 task · ✅ **Advisory Staircase edit-target — backend** (validated firm-editable routes + runtime override-merge so a firm's edit changes the complexity ceiling; +17 tests, 197 pass) · ✅ **Advisory Staircase edit-target — UI** (structured no-code editor tab in `FirmManagerHub.vue`) · ✅ **STACK-COMPLIANCE — `FirmManagerHub.vue` converted HTML→Pug.** Discovered the file was the lone component written in plain HTML, breaking Stack Constitution req. 5 (Pug mandatory); converted the entire template to Pug as a behaviour-preserving rewrite (script + scoped styles byte-identical; Pug compiles; nbsp spacers preserved). Drift found and fixed same session, not absorbed.

## Recently completed (2026-06-08)
✅ Extracted Advisory Staircase + 3 Engagement Types to JSON (Phase 1) · ✅ Fixed Growth Curve selector drift · ✅ Resolved Part 2A PDF→JSON mapping · ✅ Itemised logic trees (14 active / 28 dormant) + no-silent-parking rule · ✅ Documented all 7 non-Client functions (Part 1A) · ✅ Elevated the IDOR gap in HANDOFF · ✅ Registry final read-through + plain-language pass.
