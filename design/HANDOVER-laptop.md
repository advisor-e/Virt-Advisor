# Handover — the laptop, last session only

> **One file per machine, one session each. It is replaced each time, not added to.**
> This machine writes only this file; the desktop's is
> [`HANDOVER-desktop.md`](HANDOVER-desktop.md), and a session reads BOTH at startup.
> Anything worth keeping beyond tomorrow belongs in the feature's Brief or on
> [`features/to-do-items.json`](features/to-do-items.json). Earlier handovers are in git
> history. See [`WORKING-AGREEMENT.md`](WORKING-AGREEMENT.md).

---

## 2026-09-04 (second session) · Laptop · branch `feat/advisor-progress`

Suite **7,776 green** (402 suites), lint 0 errors, `npm run build` succeeds, first-load
**239 KB gzipped** against the 300 KB budget. **PR #58 is MERGED**; the branch is level with
`master` at `774c524` and nothing is uncommitted.

### 🖥 DESKTOP — DO THIS FIRST

🔴 **You are 36 commits behind master, and merging it in is your first move — before you
touch the forecast.** Everything the laptop has built since 2026-09-02 is now on `master`:
the volatility read, the trend read, capital assets, and the whole of buying and selling
overseas. **`ThreeWayForecastIntake.vue` and `ThreeWayForecastReport.vue` are substantially
rewritten**, and your own note puts the forecast last in 4.62 slice 2 — so the collision is
still ahead of you and still cheap.

### Built — item 4.64 finished, both slices

The **mentor's Imported Stock Prices tab** (Model Inputs, mentor alone); the **shipment
calculator** with its Restify route and screen; and **interest cover**. Every date is worked
out from the order date on Mike's own supplier terms — nothing about a date is typed.

**4.63 is CLOSED** on `to-do-done-and-parked.md`; it was built as 4.64's slice 2.
**4.64 stays OPEN for one reason only: nothing has been eyeballed.** MySQL still refuses the
placeholder password here.

### Three things worth more than the items

**The drawing contradicted itself again, and again the drawing lost.** Its shipment row let
the advisor *pick* the landing month, directly above its own ruling that every event is dated
from the order date. Found by opening the drawing beside the build plan — the fourth time
that ritual has paid on this feature.

**A balance check caught what nothing else would have, twice.** Interest cover accrued at the
landing but expensed at the payment left every month out by exactly the interest. 🔴 **Any
change that moves cash and stock in different months needs that check.**

**Two gaps were REPORTED rather than invented, and both came back as rulings** — the
workbook's 6% interest cover, and a shipping-cost formula that disagrees with its own terms
in 4 of 12 months on both supplier sheets.

### Next

**4.59** — a one-line badge fix, already proven in a sibling module. **4.66** and **4.65**
wait on Mike. **4.60** waits on four real exports from QuickBooks Online and MYOB.
