# Handover — the laptop, last session only

> **One file per machine, one session each. It is replaced each time, not added to.**
> This machine writes only this file; the desktop's is
> [`HANDOVER-desktop.md`](HANDOVER-desktop.md), and a session reads BOTH at startup.
> Anything worth keeping beyond tomorrow belongs in the feature's Brief or on
> [`features/to-do-items.json`](features/to-do-items.json). Earlier handovers are in git
> history. See [`WORKING-AGREEMENT.md`](WORKING-AGREEMENT.md).

---

## 2026-09-13 (twenty-ninth session) · Laptop · branch `feat/advisor-progress`

Suite **10,383 green** (497 suites), lint 0 errors, tree clean, **31 ahead / 0 behind**
`master`, all four commits pushed. **FIVE live items** — 4.15, 4.58, 4.86, 4.87 and the
new **4.95, which is active on THIS machine.**

🔴 **4.95 Sales Dashboard is DRAWN AND FULLY RULED, AND NOT BUILT.** The last of the three
Model Library cards that opened nothing. Drawing at
[`mockups/sales-dashboard.html`](mockups/sales-dashboard.html); **all nine decisions ruled**,
each put to Mike alone, each as recommended. **The one thing outstanding is his yes to
build** — it was asked and the session ended before he answered. **Do not start without it.**

🔴 **Three faults in the source workbook, and NONE is visible in its own sample data** — a
sale of exactly $2,500, $2,501 or $5,000 banks its money in a band and is counted in none;
the sales count reads a shorter list than every other formula on the sheet; the last
salesperson's count reads the previous person's cell. All three are ruled as deviations, to
be pinned in the golden test with the workbook's own figure beside ours.

**Decision 9 is Mike's own** — *"can we add dates"* — and it is the only part of the screen
that goes beyond the workbook. Its trend card is therefore **the one card whose shape is not
the workbook's**, marked in red on the drawing; its totals are real. ⚠ **Build note:**
`salesSheetReader.js` demands `Entry Date`, which this model never uses — its
required-columns list must become **per-model** or good files get rejected.

**Waiting on Mike, unchanged:** 4.58's OpenAI reply, 4.15's eighteen template names, whether
the unranked items get placed, and the `add-a-report` skill still telling sessions to record
work in `ACTIONS.md`, frozen since 2026-08-24.

**DESKTOP:** 4.87 untouched — none of its files were opened. **No shared code file was
touched at all this session**; only `design/` records: `ARTEFACTS.md`, `CODE-SIZE.md`,
`to-do-items.json`, `to-do.md` and the new mockup. ⚠ **Your note is still dated 2026-09-10
while your branch has a commit from 2026-09-12** — flagged three sessions running.
