# Handover — the laptop, last session only

> **One file per machine, one session each. It is replaced each time, not added to.**
> This machine writes only this file; the desktop's is
> [`HANDOVER-desktop.md`](HANDOVER-desktop.md), and a session reads BOTH at startup.
> Anything worth keeping beyond tomorrow belongs in the feature's Brief or on
> [`features/to-do-items.json`](features/to-do-items.json). Earlier handovers are in git
> history. See [`WORKING-AGREEMENT.md`](WORKING-AGREEMENT.md).

---

## 2026-09-21 · Laptop · branch `feat/advisor-progress`

**Clean, 5 ahead / 0 behind.** Suite **12,575 green** (578 suites); quick-gate clean on both
commits. Take ahead/behind from `npm run check:branch`.

🔴 **ONE THING WAITS ON MIKE AND IT IS THE ONLY THING: capture form 3 is DRAWN AND RULED BUT
NOT APPROVED TO BUILD FROM.** [`../mockups/strategy-capture-parent-child-list.html`](mockups/strategy-capture-parent-child-list.html),
published at https://claude.ai/artifact/LmibX8rxaEY6tnN23EVCRe. He ruled all five decisions
2026-09-21, each as recommended, asked one at a time — then ran `/shutdown` before answering
*"shall I build it"*. **Do not read five rulings as approval.** The question is recorded in
15.1's note and on the drawing itself.

**HE RULED THE ORG CHART IS A MINI-APP, NOT A FORM** — *"which is why the original is in a
spreadsheet"*. His file types prove it: 17 of his 20 templates are Word documents, only 2 are
spreadsheets. And the positional save guard `hasCaptureField` **would refuse role 31 on his
30-row sheet**, so a fixed grid breaks rather than merely looking wrong. **The build needs its
own record shape.**

**Measured across all 20 supplied templates, the Org Chart was the only one still reading
wrong** — 49 boxes, 32 from an empty unheaded fourth column, 42 labelled only by his row
numbers, his 24-role worked example discarded. Every other table now labels correctly.

**Two Brief defects found by the shutdown checklist and fixed:** it read *"Five stages remain
— 4b, 5, 6, 7 and 8"* while its own table records 4b built on 2026-09-20; and stage 5 said
nothing of form 3.

**DESKTOP — shared files I changed:** `design/ARTEFACTS.md`, `design/features/to-do-items.json`
(15.1's note only), `design/features/strategy-planner.md`, new
`design/mockups/strategy-capture-parent-child-list.html`, `design/CODE-SIZE.md` (generated by
`/startup`).

**`activeOn`: 7.5 and 15.1 both still laptop. Item 17 is yours** (desktop, since 2026-09-21) —
untouched here, and so is every file it names. Your handover read **current** from your own
branch in `check:branch`.

**NEXT on 15.1:** build form 3 on his yes — six forms remain after it — or start form 4 if the
answer is no.
