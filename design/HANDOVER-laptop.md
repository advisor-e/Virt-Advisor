# Handover — the laptop, last session only

> **One file per machine, one session each. It is replaced each time, not added to.**
> This machine writes only this file; the desktop's is
> [`HANDOVER-desktop.md`](HANDOVER-desktop.md), and a session reads BOTH at startup.
> Anything worth keeping beyond tomorrow belongs in the feature's Brief or on
> [`features/to-do-items.json`](features/to-do-items.json). Earlier handovers are in git
> history. See [`WORKING-AGREEMENT.md`](WORKING-AGREEMENT.md).

---

## 2026-09-14 (thirty-first session) · Laptop · branch `feat/advisor-progress`

Suite **10,517 green** (500 suites), lint 0, audit PASS. Tree clean, **6 ahead of `master`, 0
behind, everything pushed**. **Six live items.** **4.97 is ACTIVE ON THIS MACHINE.**

**NEW ITEM 4.97 — Wages/Salary Review**, asked for by Mike: *"buile the wages model into the perf
report section"*. **DRAWN AND ALL NINE DECISIONS RULED, NOTHING BUILT.** The drawing is
[`mockups/wages-model.html`](mockups/wages-model.html); the rulings are on it and in
[`ARTEFACTS.md`](ARTEFACTS.md). The workbook was **not in this repo** — Mike supplied its location
and it is now `report-source-models/Wages Model.xlsx`.

🔴 **WAITING ON MIKE, AND IT IS THE NEXT THING TO ASK:** approval to build FROM the drawing. Ruling
on the nine questions inside it is **not** the same as approving it — he has not been asked. The
recommendation put to him was to start with the labour-margin engine and its golden test.

🔴 **MIKE MUST SEND A REAL PAYROLL EXPORT.** Decision 4 added a payroll import he asked for; per
`supportedPackages.js` no reader may be called supported until a real export is read. Nothing else
blocks it.

⚠ **FOUR OF THE NINE RULINGS WENT AGAINST THE RECOMMENDATION** — build from the rulings, not the
recommendations. The name and section (**Wages/Salary Review**, **Decision tools**, tag
*Profitability*), the payroll import, income tax bands as a fifth **Tax Rates** figure *never
withheld*, and the register kept on a **retention dial** rather than deleted at deal-end.

🔴 **A LESSON THAT APPLIES TO EVERY WORKBOOK PORT, not just this one.** Mike: *"don't assume that
things like 'wet n dark' days are a set figure - you will see this throughout the entire xisting
model."* Classify every cell before drawing: `<f>` means calculated, no `<f>` means typed. This
workbook is **1,778 typed against 8,714 calculated**. Three claims on the first drawing were wrong
because of it.

**DESKTOP:** 4.87 untouched — none of its files opened. **Only `design/` files changed** this
session: the new drawing, `ARTEFACTS.md`, `features/to-do-items.json`, `to-do.md`, `CODE-SIZE.md`
(generated by `npm run handbook`) and the workbook. **No code, no tests, no locales.** ⚠ **Your note
is still dated 2026-09-10 while your branch has a commit from 2026-09-12** — flagged five sessions
running.
