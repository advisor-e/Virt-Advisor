# Deployed Versions — the ledger

> **Version-Pull Recording Rule (binding).** Any time this repository's code is pulled,
> installed, or updated in any environment beyond a developer's own machine — UAT,
> production, a demo, or inside the Advisor-e master app — the person doing it must
> record it **at that moment** in the table below: the date, the environment, the exact
> commit hash pulled, who pulled it, and any notes. **A deployment is not complete until
> its row is written.** This is how everyone always knows which version is running
> where, and in what state.

How to find the commit hash you are pulling: `git rev-parse HEAD` immediately after the
pull, or read it from the GitHub commits page. Record the full state honestly — if the
hash wasn't captured at the time, add the row anyway with "commit unknown" and backfill
it as soon as it can be established (see the open rows below).

| Date | Environment | Commit | Pulled by | Notes |
|---|---|---|---|---|
| 2026-07-14 | UAT (inside Advisor-e master app) | `709bac5` — PR #2, `feat/client-knowledge-base` | Master-app team | Confirmed by the master team 2026-07-21. 97 commits behind `origin/master` at the time of confirmation. |
| 2026-08-04 | Local (master-app team machine) | `2beba9f` — `v0.7.0` plus two documentation-only commits | Carl Allado (master-app team) | Reported to Mike by email 2026-08-09; backfilled the same day. **First uptake of any release tag from this repo.** Contains the whole `v0.7.0` release — the tagged commit is `015eed0`, and the only commits between it and `2beba9f` are `32a277f` + `2beba9f` itself, both release-notes documentation. ⚠ **`npm install` required and NOT yet confirmed with Carl** — `v0.7.0` adds the `@mdi/font` dependency, and without it the Hub's tab icons render blank, which reads as a broken build rather than a missing package. ⚠ Pulled from `master` at a point in time rather than from the `v0.7.0` tag (Working Agreement asks for the tag, because a branch keeps moving and a tag does not); content is identical here bar the two doc commits, so nothing is wrong — noted so the next pull is easier to trace. At the time of this pull `origin/master` was 2 commits further on (`6f44872`, `39c274a`). |

## Releases offered (not yet a deployment)

A tag existing is **not** a deployment. Rows go in the table above only when code is
actually pulled into an environment. This section records what has been cut and handed
over, so an unanswered release is visible rather than assumed.

| Tag | Commit | Cut | Offered to | Status |
|---|---|---|---|---|
| `v0.13.0` | `9b44cbd7` | 2026-09-16 | Master-app team | ✅ **CUT AND PUSHED 2026-09-16, CONFIRMED ON `origin`** — the tag is on `9b44cbd7`, the merge commit of [PR #97](https://github.com/advisor-e/Virt-Advisor/pull/97) (the release paperwork carrying the `0.13.0` version number), which sits directly on top of [PR #96](https://github.com/advisor-e/Virt-Advisor/pull/96) — the 197 commits themselves. **Awaiting pull.** *(Row written ahead of the tag and backfilled the moment it was cut — the same discipline v0.8.0 onward used.)* 197 commits since v0.12.0, from both machines. 🔴 **THIS ONE NEEDS A DATABASE CHANGE — THE FIRST SINCE v0.11.x. ONE NEW TABLE: `advisor_model_choices`.** `config/db-schema.sql` gained **59 lines and nothing else** — purely additive, nothing altered, renamed or dropped, so existing data and every other table are untouched. Run that block against the UAT database. **If it is skipped:** the Model Choices record writes nothing and its screen stays empty; everything else runs normally and it is NOT a boot failure. ✅ **NO `npm install`** — `package.json` and `package-lock.json` differ from v0.12.0 only in `version`. 🔴 **First check after installing: `npm run backend` should print `[restify] virt-advisor-api listening on …`.** If it exits instead, stop — no screen is worth opening. **THE ENGINE'S MIDDLE IS THE HEADLINE AND IT CHANGES WHAT AN ADVISOR IS SHOWN** (item 7.2): the engine now asks the advisor what the problem actually is rather than inferring it from one category word, learning can lift a template as well as hold it back, the bench reports out-of-sample honestly, and a provider outage no longer stops every AI feature — the advisor is told which AI answered them. **Template Profiles**, a new Mentor Hub tab, shows what the AI understands each of the 220 client tools to be *about* — the resolver's dominant lever, visible on no screen until now — and lets the mentor author it, with every save restorable. Also **Wages/Salary Review** (5.1) and the **Model Choices** record above. ⚠ **Known before testing:** eight of nineteen calculators are not offered when an advisor asks the question they answer (7.9, measured over 30 live conversations); the middle-tier role values are still Advisor-e's to supply, so those hubs fail closed by design; the Adviser Network runs on invented people and persists nothing in production mode (11.1); 14 logic-table branches name documents the library does not hold, so that coaching is withheld (7.1). Notes: [`RELEASE-NOTES-v0.13.0.md`](RELEASE-NOTES-v0.13.0.md). Verified before the tag, **on the exact merge commit the tag sits on**: 11,930 tests green / 556 suites, lint 0 errors, critical-audit gate PASS, `nuxt build` exit 0, **and the backend started and seen listening**. |
| `v0.12.0` | `14546d8f` | 2026-09-13 | Master-app team | ✅ **CUT AND PUSHED 2026-09-13** — the tag is on the merge commit of [PR #92](https://github.com/advisor-e/Virt-Advisor/pull/92), confirmed on `origin`. **Awaiting pull.** *(Row written ahead of the tag and backfilled the moment it was cut — the same discipline v0.8.0 onward used.)* 92 commits since v0.11.1, all from the laptop. 🔴 **SIX NEW MODELS, AND THE MODEL LIBRARY HAS NO EMPTY CARDS LEFT** — the High-Level Budget (4.88), the Retirement Review (4.90), the Mid-Level Budget (4.93), Stock Purchasing (4.94) and the Sales Dashboard (4.95); **18 of 18 models now open something**, where three used to say *"coming soon"* and do nothing. Also country rate schedules loaded once at global group manager (4.92), and **a cap of 20 paid AI document readings per firm per rolling 24 hours** (4.82), which nothing limited before. ✅ **NO `npm install`** — `package.json` and `package-lock.json` differ from v0.11.1 only in `version`. **No table changed** — `config/db-schema.sql` is byte-identical to v0.11.1. 🔴 **First check after installing: `npm run backend` should print `[restify] virt-advisor-api listening on …`.** If it exits instead, stop — no screen is worth opening. 🔴 **SEVERAL SOURCE WORKBOOKS CONTAIN REAL ERRORS AND WE DO NOT REPRODUCE THEM** — every deviation was ruled by Mike on the drawing before any code, and each is pinned in a golden test with the workbook's own figure beside ours. **The Sales Dashboard's three are invisible in its own sample data**: a sale of exactly $2,500, $2,501 or $5,000 banks its money in a band and is counted in none; the headline sales count reads a list 21 rows shorter than the money does, so past ~491 sales the average sale value climbs for no reason; and one salesperson's count reads the previous person's cell. ⚠ **Known before testing, and unchanged by this release:** the Adviser Network runs on invented people and persists nothing in production mode (4.86); the two middle-tier hubs fail closed until Advisor-e supplies their role values; 14 logic-table branches name documents the library does not hold, so that coaching is withheld (4.15); **Meeting Review is code-complete and must NOT be used on a real client** — three non-coding gates remain (4.58); and **nothing here has been run against MySQL**, so real persistence is untested on our side and is the first thing to exercise in UAT. ⚠ **The desktop's Course Builder and quiz-builder work is NOT in this release.** Notes: [`RELEASE-NOTES-v0.12.0.md`](RELEASE-NOTES-v0.12.0.md). Verified before the tag: 10,507 tests green / 500 suites, lint 0 errors, critical-audit gate PASS, `nuxt build` exit 0, **and the backend started and seen listening**. |
| `v0.11.1` | `e7e6271` | 2026-09-10 | Master-app team | ✅ **CUT AND PUSHED 2026-09-10** — the tag is on the merge commit of [PR #79](https://github.com/advisor-e/Virt-Advisor/pull/79), confirmed on `origin`. **Awaiting pull.** *(Row written ahead of the tag and backfilled the moment it was cut — the same discipline v0.8.0 onward used.)* 🔴 **IT REPLACES `v0.11.0`, WHICH DOES NOT START** — see the row below. It contains the whole of v0.11.0 plus the boot fix and item 4.84 (notification dots on every hub tab). ✅ **NO `npm install` over a v0.11.0 tree** — `package.json` and `package-lock.json` differ only in `version`; a fresh clone follows the v0.11.0 instructions (npm 8 on Node 14.15). **No table changed** — `config/db-schema.sql` is byte-identical to v0.11.0. 🔴 **First check after installing: `npm run backend` should print `[restify] virt-advisor-api listening on …`.** If it exits instead, stop — no screen is worth opening. ⚠ **Every hub tab will show a blue dot on first sight, at every tier.** That is correct: the app has no record of anyone opening anything until this release starts keeping one, and it clears as a manager goes round. Everything in §7 of the v0.11.0 notes still stands. Notes: [`RELEASE-NOTES-v0.11.1.md`](RELEASE-NOTES-v0.11.1.md). Verified before the tag: 9,749 tests green / 475 suites, lint 0 errors, critical-audit gate PASS, `nuxt build` exit 0, **and the backend started and seen listening** — the check that did not exist when v0.11.0 was cut. |
| `v0.11.0` | `0f0fdab` | 2026-09-10 | Master-app team | 🔴 **WITHDRAWN 2026-09-10, THE DAY IT WAS CUT — DO NOT PULL. IT DOES NOT START.** One route handler in the compliance gate was an `async` function that also took Restify's `next`; Restify refuses to *mount* such a handler and asserts at mount time, so the backend threw while registering routes and exited. **Not one broken route — no routes at all and no API.** Nuxt starts normally with nothing behind it, so it looks alive and every screen fails to load. Found by starting the app, hours after the tag, and superseded by **`v0.11.1`** above; if this tag has been pulled anywhere, replace it rather than patch it. ⚠ **It was verified before tagging and every gate passed** — 9,694 tests, lint 0, audit PASS, `nuxt build` exit 0 — because not one of those things starts the server. That gap is now closed by `tests/unit/serverMounts.test.js` **and** by running the app before a tag is cut. The row below is kept as written at the time. ~~✅ **CUT AND PUSHED 2026-09-10**~~ — the tag is on the merge commit of [PR #75](https://github.com/advisor-e/Virt-Advisor/pull/75), confirmed on `origin`. **Awaiting pull.** *(This row was written ahead of the tag, marked not-yet-cut, and backfilled the moment it was — the same discipline v0.8.0 to v0.10.0 used.)* 484 commits since v0.10.0 from both machines: the **Business Performance Report** finished and walked on a real export; **Meeting Review** complete on the advisor's side down to the individual client, with the client's right to a copy; **Compliance pages** with a firm's declaration gating Meeting Review; **Depreciation Rates** and **Tax Rates** per country, approved from the tax authority's documents; **economic analysis** on the forecast; the **template library in the database** with a push endpoint for Advisor-e; the tier vocabulary corrected. 🔴 **THIS ONE NEEDS `npm install`, RUN WITH npm 8 ON Node 14.15** — `engine-strict` is on and npm 6 or 10 will not do; no browser downloads. **No table changed.** 🔴 **Known before testing:** the laptop's month of screens (client register, Depreciation Rates, Tax Rates) has never been opened in a browser and needs MySQL — open them first; the middle-tier role values are still Advisor-e's to supply; the Adviser Network runs on invented people until identity is wired (4.86). Notes: [`RELEASE-NOTES-v0.11.0.md`](RELEASE-NOTES-v0.11.0.md). Verified before the tag: 9,694 tests green / 471 suites, lint 0 errors, critical-audit gate PASS, `nuxt build` exit 0 on this machine. |
| `v0.10.0` | `458cf9e` | 2026-08-22 | Master-app team | ✅ **CUT AND PUSHED 2026-08-22** — the tag is on the merge commit of [PR #45](https://github.com/advisor-e/Virt-Advisor/pull/45), confirmed on `origin`. **Awaiting pull.** *(This row was written ahead of the tag, marked not-yet-cut, and backfilled the moment it was — the same discipline v0.8.0 and v0.9.0 used.)* 59 commits since v0.9.0: the **Multiple Property Assessment finished** as a five-property household portfolio; its **tax rules became settings** a group sets and a firm corrects; the Firm Manager Hub menu regrouped with a duplicate tab removed; a new **AI Prompts** tab at all four manager tiers, where the method is read-only and the platform protocols are prepended on the backend so no screen can reach them; and the **ten built calculators, which the backend had never heard of**, are now something the AI can point an advisor at. 🔴 **THIS ONE NEEDS `npm install`** — one devDependency (`playwright`, exact `1.34.3`) plus `.npmrc` and `package-lock.json`. **It downloads no browser:** `playwright_skip_browser_download=1` suppresses the 604 MB fetch, and `npm ci --omit=dev` skips it entirely. No runtime dependency changed. 🔴 **Known issues worth reading before testing:** this app has not yet been told which role values the two middle-tier managers' tokens carry, so their hubs fail closed here (*corrected 2026-09-02 — this row previously said those managers "cannot be logged into", which is wrong: they log into Advisor-e and have for 18 months*), and the engine can still route a question to the wrong coaching tree and invent content that reads as authored — §7 of the notes. Notes: [`RELEASE-NOTES-v0.10.0.md`](RELEASE-NOTES-v0.10.0.md). Verified before the tag: 6,037 tests green / 326 suites, lint 0 errors, critical-audit gate PASS. |
| `v0.9.0` | `d4284e6` | 2026-08-17 | Master-app team | ✅ **CUT AND PUSHED 2026-08-17** — the tag is on the merge commit of [PR #44](https://github.com/advisor-e/Virt-Advisor/pull/44), confirmed on `origin`. **Awaiting pull.** *(This row was written ahead of the tag, marked not-yet-cut, and backfilled the moment it was — the same discipline v0.8.0's row used.)* 85 commits since v0.8.0: **102 pieces of authored advisory content that reached no prompt at all**, now reaching the model AND appearing on a screen for the first time — the thirteen method guides (155,000 characters that no screen anywhere rendered), the 65 diagnostic branches, the 26 entry questions, the 13 stage-entry questions, the staircase selector question; plus a new **Coaching Reference** tab on every management hub. ✅ **NO `npm install` — no dependency added, removed or moved;** `package-lock.json` untouched and `package.json` differs only in `version` and three developer-only `scripts`. 🔴 **Known issue worth reading before testing:** the engine can route a question to the wrong coaching tree, and the AI then INVENTS content that reads as authored — §4a of the notes. Notes: [`RELEASE-NOTES-v0.9.0.md`](RELEASE-NOTES-v0.9.0.md). Verified before the tag: 5,745 tests green / 321 suites, lint 0 errors, critical-audit gate PASS, `nuxt build` green (exit 0, zero warnings) on this machine. |
| `v0.8.0` | `e3b7a21` | 2026-08-13 | Master-app team | ✅ **CUT AND PUSHED 2026-08-13** — the tag is on the merge commit of [PR #41](https://github.com/advisor-e/Virt-Advisor/pull/41), confirmed on `origin`. **Awaiting pull.** *(This row was written ahead of the tag, marked as not-yet-cut, and backfilled the moment it was.)* 74 commits since v0.7.0: the management cascade reaching all six levels and its two new hub pages, every report rolling up one level, the Template Check screen, the 55 branches whose instruction reached the AI nowhere, and the storage fix below. ✅ **NO `npm install` — `package.json` is byte-identical to v0.7.0**, unlike v0.7.0 which needed one and confused the first pull. 🔴 **The line that matters for testing: a save the database REFUSED used to be written to a scratch file and reported as saved, in any environment not named exactly `production` — so a UAT test could pass having never written to the database.** Fixed. Notes: [`RELEASE-NOTES-v0.8.0.md`](RELEASE-NOTES-v0.8.0.md). Verified before the tag: 5,114 tests green / 300 suites, lint 0 errors, critical-audit gate PASS, `nuxt build` green on this machine. |
| `v0.7.0` | `015eed0` | 2026-08-04 | Master-app team | ~~Awaiting pull~~ **PULLED 2026-08-04 by Carl Allado — see the row above.** Taken as `2beba9f` (the tag plus two documentation-only commits) rather than as the tag itself. 379 commits since v0.6.0: Logic-Lab (incl. the accept button, live-proven), the silent-AI-failure honesty fixes, the Course Builder session slicer, the browser-trust security fix, trace i18n. **New dependency `@mdi/font` — `npm install` required.** Notes: [`RELEASE-NOTES-v0.7.0.md`](RELEASE-NOTES-v0.7.0.md). Verified at tag time: 4,597 tests green / 267 suites, lint 0 errors, critical-audit gate PASS. **Supersedes the unpulled v0.6.0** — pulling v0.7.0 covers both, so v0.6.0 will now never be pulled and that is correct. |
| `v0.6.0` | `9a29aee` | 2026-07-21 | Master-app team | ~~Awaiting pull~~ **Superseded by v0.7.0 before it was ever pulled.** First tag this repo carried. 174 commits ahead of the `709bac5` UAT build, first release containing Course Builder. Notes: [`RELEASE-NOTES-v0.6.0.md`](RELEASE-NOTES-v0.6.0.md). Verified at tag time: 1,456 tests green, lint clean, `nuxt build` green on Node 14.15. |

**Production: nothing has been deployed to production yet.** The app is in UAT testing
only. The first production row will be added when a production deployment actually
happens. Any earlier note in this repo describing a 2026-07-13 production go-live was
incorrect and is withdrawn (master-team clarification, 2026-07-21).
